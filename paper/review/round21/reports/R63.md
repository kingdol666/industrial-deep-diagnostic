# R63 — FIGURES & VISUAL COMMUNICATION Review (Round 21, blind)

Reviewer scope: every figure (Figs 1–8) and table (Tables 1–10, A1, A2) judged **as rendered
images** (110 dpi page PNGs `paper/review/round21/pages/p-001..075`, current build 09-17 07:18,
identical timestamp to `main.pdf`), cross-checked against the figure sources
(`paper/figures/make_figures.py`, TikZ in `paper/main.tex`) and repository ground truth.
Blindness rule respected: nothing else under `paper/review/` was read.

---

## 1. Figure-by-figure judgement (rendered images)

### Fig 1 — Skill execution architecture (TikZ, p13)
- Five stacked planes, arrows clean, no overlaps, no text overflow.
- Printed text size measured from the PDF text layer (`main.pdf` p13): **6.5 pt** for all node
  text incl. the 14-engine list. Exactly at the ≥6.5 pt floor — acceptable, no headroom.
- Caption–figure agreement: 14 engines / 18 packages / CP-1..CP-9 / audit plane all present as
  captioned. PASS (borderline size noted).

### Fig 2 — Diagnostic pipeline flow (TikZ, p15) — **FAILS the legibility floor**
- Rendered crisply; 4x zoom confirms **no overlaps**: the dashed amber repair edge, its italic
  label "repair: best-of-3, global ≤5", the Step-6 entry arrow and the deliverables row are all
  cleanly separated. Caption–figure agreement good (amber CP tags, green hard gates 7/8.5/9,
  right-to-left bottom row, best-of-3/global-5/0.50 counters as captioned).
- **Defect (measured, not eyeballed):** the PDF text layer of p15 reports every in-figure span
  ("Step 0/1", "main agent · data ingestion", "CP-1", "repair: best-of-3, global", "deliverables:")
  at **6.01 pt** — i.e. the `\resizebox{\textwidth}` is still *down-scaling* the picture by 0.75
  (natural 8 pt scriptsize → 6.01 pt printed). This is below the 6.5 pt acceptance floor and well
  below the 7 pt ideal. Pixel measurement corroborates (cap 'S' ≈ 6–7 px at 110 dpi ≈ 4.1–4.6 pt
  cap height ≈ 6–6.7 pt em).
- Fix (cheap): shorten box texts (e.g. drop the parenthetical "(CP-2/3)" etc. into the CP tags),
  reduce `node distance` and `minimum width`, or split the deliverables line — anything that
  shrinks natural width so the rescale lands ≥1.0; alternatively set node font to `\small` so
  post-scale ≥ 7 pt. Target: printed ≥6.5 pt, ideally ≥7 pt.

### Fig 3 — Benchmark outcomes (matplotlib, p38)
- (a) SKAB bar fully hatched sky with bold "0/2"; TEP solid blue 5 + amber 1 with "5/6";
  IndPenSim solid blue "1/1". Sums to the 6/9 headline. Fractions verified against
  `results/benchmark/metrics.json` (top1=6, fault_cases=9). Hatch (black '///' on sky) clearly
  visible at print size and survives grayscale (sky lum ≈158 vs amber ≈202 vs blue ≈62).
- (b) filled rubric vs open judge markers, blue faults / grey controls, dotted "gate 90" — matches
  caption; deducible points (85 SKAB valve, 90 IDV11) consistent with Table 8.
- Legends deliberately omitted, semantics carried by the caption — acceptable and stated.
- PASS.

### Fig 4 — Confidence per scenario (matplotlib, p40)
- Colour/hatch convention exactly as briefed: solid dark blue = DET faults; hatched sky = capped
  CS ranked (SKAB valve 0.65, SKAB cavitation 0.58); solid amber with near-black text = capped CS
  unranked (IDV3 0.60); hatched grey = three controls (0.78/0.88/0.88); dashed amber line at 0.70
  labelled "ceiling for capped verdicts only (0.70)".
- **White bboxes on value labels verified in the zoom**: "CS 0.60" sits hard against the dashed
  ceiling line and no dash strikes the glyphs; "normal 0.78/0.88" labels crossing the dashed line
  are equally protected. Confidences verified against released `confidence.json` files (65, 60,
  75 spot-checked). PASS.

### Fig 5 — SKAB valve case study (matplotlib, p41)
- In-panel annotations ("anomaly window (rows 631–919) / 210 switches 30↔31"; "lowest quantisation
  bin (30 L/min): 125 rows, 106 runs, mean run 1.18 samples"; "pump load 0.947 A in-window vs
  0.990 A outside (−4.3%)") all read directly from the released run artifacts — **all eight
  numbers match** `202609150453290_bench_skab_valve1_1/02_processed/anomaly_segment_analysis.json`.
- **Caption names both floor-bin measures** (125 rows/106 runs *and* the 39.4% floor-residence
  metric) "reported separately, not equated" — exactly as required; both figures also appear in
  the in-figure evidence strip. Pale-amber window / pale-green floor band as captioned. PASS.

### Fig 6 — Per-fault TEP matrix (matplotlib, p46)
- Cells match caption and Table A1: vermillion "wrong" only at FE GPT-4o × IDV1; green "correct";
  pale grey "unscored"; PCA "no" cells grey; IDD column dark blue Top-1 (0.79/0.75/0.82/0.80/0.89)
  + amber "capped CS (0.60)" with dark text. The old double-green collision is gone (IDD blue
  distinct from FE green). PASS.

### Fig 7 — Forest plot (matplotlib, p47)
- Six rows; printed labels 7/11 [35.4, 84.8]; 9/11 [52.3, 94.9]; 5/6 [43.6, 97.0]; 6/9 [35.4, 87.9];
  8/9 [56.5, 98.0]; **controls 3/3 [43.8, 100.0]** — identical to Table 7, z = 1.96 stated in the
  caption. I recomputed the Wilson intervals by hand at z=1.96: all six match to 0.1 pp.
- Grey/blue/green = FE / IDD / controls as captioned; the "labels for 100% rows extend past the
  axis" behaviour is disclosed and renders cleanly outside the spine. PASS.

### Fig 8 — Suite matrix (matplotlib, p56)
- Column 1 convention as briefed (hatched sky ranked CS ×2, amber unranked IDV3, blue Top-1, green
  normal); hatch visible. Columns 4–5 teal ramp (orthogonal to semantic hues) with printed rates
  that match `results/benchmark/baselines.json` exactly: IDV3 3/5 (0.0288/0.0475), IDV4 49/100
  (0.488/1.0), IDV11 54/66, IDV14 100/93, IDV1 99/100, batch93 15/74, controls 1/1. Column 6
  vermillion "no" only at IDV3; "no (correct: control)" grey at the TEP control. Legend present.
- Cosmetic: TEP appears in **two row blocks** (IDV1/IDV3/TEP-control above the IndPenSim block,
  IDV4/7/11/14 below); the group separators mark it, but the caption does not say why the ordering
  is not dataset-contiguous. One clause in the caption would fix it.

## 2. Tables (all rendered, all legible)

- **Table 1** (p8): clean; narrow column forces "TEP se-ries" mid-word hyphenation — cosmetic.
- **Table 2** (p17): full float page; scriptsize monospace paths legible; wrap "Visual evi-dence"
  slightly awkward — cosmetic. Gate column consistent with Table 3.
- **Table 3** (p18): clean; sits alone on a float page with ~55% blank below (see §4).
- **Table 4** (p26): chronology legible; timestamps match the journal (20:06:32Z verified).
- **Table 5** (p29): 14 artifacts, clean.
- **Table 6** (p32): clean; totals 9+3 match the text.
- **Table 7** (p37): all CIs present; controls [43.8, 100.0] identical to Fig 7; z=1.96 stated.
  "ten at 100, one at 90, one at 85" consistent with Table 8 (8 faults+3 controls → ten 100s).
- **Table 8** (p43): every verdict/confidence/rubric value agrees with Figs 3b/4/6/7 and §7 text
  (0.65/85, 0.58/100, 0.79, 0.60/100, 0.75/100, 0.82, 0.80/90, 0.89, 0.88, controls 0.78–0.88).
- **Table 9** (p49): clean; IDD row line-break "COMPETING _SET)" slightly odd — cosmetic.
- **Table 10** (p52): Δconf −0.09/+0.07/0.00 consistent with §8.4 text.
- **Table A1** (p72): 5/6 (CI [43.6, 97.0]), 4/4 shared subset — matches §8.1 and Fig 6.
- **Table A2** (p73): clean artifact index; both appendix tables share float pages as intended.

## 3. Colour / grayscale / convention audit

- IDD dark blue = determined everywhere (Figs 3, 4, 6, 8); batch-93 no longer green. GREEN
  reserved for FE-correct (Fig 6), bare-LLM hits (Fig 8) and controls (Figs 7, 8). AMBER
  (near-black/dark text) = capped CS unranked; hatched SKY = capped CS ranked — **captions say
  "hatched" wherever hatching is used** (Figs 3, 4, 8). Consistent.
- Grayscale printing: sky lum ≈158 + black '///' hatch vs amber ≈202 (no hatch): separable by both
  pattern and Δlum ≈44; blue ≈62 vs sky ≈158 separable. The suite-matrix teal ramp (≈white→dark
  teal) reuses no semantic hue. Print-safe.
- CIs: z = 1.96 in Table 7, Fig 7 and §6.2 text; make_figures.py:74 `wilson(x, n, z=1.96)`;
  controls [43.8, 100.0] identical in Table 7 and Fig 7. Consistent everywhere checked.

## 4. Float discipline

- All floats top-aligned (`\@fptop=0pt`), captions below figures / above tables per Elsevier
  convention. No float appears before its first citation page-range, and `placeins [section]`
  keeps everything inside its section.
- Isolated section-end stub pages exist and are the one layout blemish:
  - **p18**: Table 3 alone, ~55% of the page blank.
  - **p42**: §7 tail text ends mid-page, ~40% blank (Table 8 float page follows).
  - **p55**: worst case — only 4 lines of §8.5 text, then ~85% blank (Fig 8 float page p56).
  Judged acceptable for a 12 pt preprint (arXiv-style previews look like this all the time), but
  the p55 stub is ugly and trivially avoidable by declaring Fig 8 before §8.4 so it lands on p54/55
  top, or by a judicious `\clearpage`. Cosmetic, not blocking.

## 5. Figure–text number spot-checks (7 performed, all MATCH)

1. Fig 3a fractions 0/2 + 5/6 + 1/1 vs `results/benchmark/metrics.json` top1=6/9 — match.
2. Fig 3b/Table 8 rubric 100×10, 90 (IDV11), 85 (SKAB valve) vs Table 7 row — match.
3. Fig 4 confidences (0.65/0.58/0.60; DET 0.75–0.89; normal 0.78/0.88/0.88) vs released
   `confidence.json` (65, 60, 75 verified directly; rest via Table 8) — match.
4. Fig 5 annotations (631–919 / 289 rows / 210 switches / 125 rows / 106 runs / mean 1.18 /
   0.947 vs 0.990 A / −4.3% / 39.4% residence) vs run-dir `anomaly_segment_analysis.json` — all match.
5. Fig 7 six CI pairs vs hand-recomputed Wilson z=1.96 (e.g. 7/11 → [35.4, 84.8]) — match.
6. Fig 7/Table 7 controls 3/3 [43.8, 100.0] — identical, match.
7. Fig 8 PCA cells (3/5, 49/100, 54/66, 100/93, 99/100, 15/74, 1/1) vs `baselines.json` — match.

## 6. Claim-vs-reality spot checks (≥3, with file:line)

1. **"Wilson 95% intervals at z=1.96 (the released scorer's constant)"** (Table 7 caption, §6.2) →
   `scripts/benchmark/aggregate.mjs:28` `function wilson(k, n, z = 1.96)` and
   `paper/figures/make_figures.py:74` `def wilson(x, n, z=1.96)` — **match** (and Fig 7 values
   reproduce under that constant).
2. **Fig 5 "every annotation is read from the released run artifacts"** →
   `workspace/diagnostic-runs/202609150453290_bench_skab_valve1_1/02_processed/anomaly_segment_analysis.json`
   (index_range [631,919]; flow_in_window.switch_count_30_31 = 210; value_counts["30.0"] = 125;
   n_runs_at_30 = 106; mean_run_len = 1.18; detection string "…39.4% residence at lowest quantized
   level vs 0.0% outside"; current_in_vs_out 0.947/0.9898) — **match** (script
   `make_figures.py::fig_case_study` reads exactly these keys; no hand-copied values found).
3. **Fig 4/§7.3 "IDV4 binds DETERMINED at 0.75"** →
   `workspace/diagnostic-runs/202609141854259_bench_tep_d04_reactor_cooling_step/04_diagnostics/confidence.json`
   `overall_confidence.score = 75` — **match**.
4. **Table 4 chronology "Pre-switch batch 09-14 20:06:32Z"** →
   `results/benchmark/journal.jsonl` earliest stamps `2026-09-14T20:06:32.635Z…` — **match**.
5. **Fig 8 columns read from released baselines** → `results/benchmark/baselines.json`
   `pca.tep_d03_hard.detection_rate_T2 = 0.0288 / SPE = 0.0475` printed as 3/5;
   `pca.tep_d04_reactor_cooling_step` 0.488/1.0 printed 49/100 — **match**.

## 7. Defect list

**Blocking (1):**
- B1. **Fig 2 (p15): in-figure text prints at 6.01 pt** (measured from the p15 PDF text layer;
  every span 6.01 pt). Below the stated ≥6.5 pt floor; the italic repair label and the
  deliverables line are the worst affected. Fix: shrink natural TikZ width (shorter box labels,
  tighter node distance) or raise node font so post-resize ≥6.5 pt, ideally ≥7 pt. One hour of work.

**Cosmetic (5):**
- C1. p55 stub (4 lines + ~85% blank) from the Fig 8 float page; p18 float page ~55% blank;
  p42 ~40% blank. Acceptable preprint typography, but p55 is worth a placement tweak.
- C2. Fig 8 caption does not explain the non-contiguous dataset ordering (TEP in two blocks).
- C3. Table 1 "TEP se-ries" mid-word hyphenation in a narrow column.
- C4. Table 9 IDD row line-break "COMPETING _SET)" — add `\mbox` or `~` before the underscore token.
- C5. Fig 1 at exactly 6.5 pt — legal but no margin; consider the same treatment as B1 if touched.

## 8. Bottom line

Seven of eight figures and all twelve tables are print-clean: colour conventions are applied
consistently, hatching carries the ranked/unranked distinction in grayscale, value labels are
protected by white bboxes, every caption matches its figure, and every number I traced — 7
figure–text spot-checks and 5 claim-vs-reality checks — reproduces from the released artifacts.
The single blocker is Fig 2's 6.0 pt printed text, which misses this round's explicit 6.5 pt
legibility floor; with that one (mechanical) resize, the figure programme is print-clean for AEI.
