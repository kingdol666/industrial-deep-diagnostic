# R35 — Figures & Visual Communication Review (Round 14, blind)

Reviewer scope: every figure (Figs 1–8) and table (Tables 1–9, A1–A2) judged **as rendered images**
(`paper/review/round14/pages/p-*.png`, 110 dpi), plus the figure programme conventions
(`paper/figures/make_figures.py`), caption–figure agreement, legibility (incl. grayscale/hatch),
figure–text number consistency, and float discipline.

Float→page map taken from `main.aux`: T1 p.8; F1 p.13; F2 p.15; T2 p.17; T3 p.18; T4 p.28; T5 p.31;
T6 p.36; F3 p.37; F4 p.39; F5 p.40; T7 p.42; F6 p.45; F7 p.46; T8 p.48; T9 p.51; F8 p.55;
TA1 p.71; TA2 p.72. All 8 figures + 11 tables opened and inspected (zoomed crops where needed).

---

## 1. Assigned convention checks — all verified

| Required convention | Rendered evidence | Verdict |
|---|---|---|
| IDD dark blue = DETERMINED everywhere | F3a blue segments; F4 solid blue bars ("DET x.xx" dark-blue labels); F6 IDD column blue; F8 col-1 blue cells. Batch-93 is blue in F4/F8 (script comment at `make_figures.py:186-189` confirms the earlier green was unified). | PASS |
| Hatched sky = capped CS ranked (Figs 3a/4/8); captions must say "hatched" | F3a p.37: SKAB bar visibly black-hatched sky; caption says "**hatched sky blue**" verbatim. F4 p.39: both SKAB bars hatched; caption says "hatched sky-blue bars" (0.65/0.58 named). F8 p.55: col-1 sky cells hatched; caption says "hatched sky capped COMPETING_SET"; legend swatch hatched. | PASS |
| Amber with near-black text = capped CS unranked | F3a amber segment (IDV3); F4 amber bar with dark-brown "CS 0.60" (readable in zoom, `#3a2600`/`AMBER_DARK` in code); F6 "capped CS (0.60)" amber cell; F8 "CS 0.60 unranked" amber cell, dark text. | PASS |
| Green = FE hits / control cells | F6 FE-correct cells green (vermillion for the single "wrong"); F7 controls row green; F8 green "normal"/"hit"/"det." cells. | PASS |
| Grey = FE/unscored | F6 pale-grey unscored cells + grey "no" (PCA-excluded); F3b grey control markers; F4 hatched grey control bars; F8 pale-grey "not in protocol scope". | PASS |
| Fig 4's 0.70 line draws beneath bars | Zoom of p.39: amber dashed line visible in headroom band and inter-row gaps, hidden where bars cross it (zorder 0.5 at `make_figures.py:194`). In-figure tag "ceiling for capped verdicts only (0.70)" agrees with caption. | PASS |
| Fig 5 caption names the two floor-bin measures | p.40 caption names both: "125 samples at the 30.0 L/min quantisation level — the in-panel annotation's row count and its 106 runs — whereas the pipeline's separate floor-residence metric reports 39.4% … named and reported separately, not equated." Panel annotation and evidence strip print exactly these two numbers. | PASS |
| Fig 7 and Table 6 both print controls CI [43.8,100.0] at z=1.96 | Fig 7 p.46 prints "3/3 = 100.0% [43.8, 100.0]"; Table 6 p.36 prints "Control pass (no false alarm; n=3) 3/3 [43.8, 100.0]" (`main.tex:446`). Recomputed with the script's own formula: wilson(3,3,1.96) = (43.85→43.8, 100.0). `make_figures.py:74` `z=1.96`; released scorer `scripts/benchmark/aggregate.mjs:28` `z = 1.96`. **Consistent.** | PASS |
| Fig 8 y-labels ≥6.5pt effective | `make_figures.py` sets figure width = PRINT_W 5.4 in = final print width; measured text block in render = 592 px / 110 dpi = 5.38 in, so nominal ≈ printed size. y-labels fontsize 7 (`make_figures.py:384`) → ≈7 pt effective, ≥6.5. Legible in the 110 dpi render and in zoom. | PASS |

## 2. Caption–figure agreement (all floats)

- **F1** (p.13): "Fourteen engines / 18 skill packages / nine-stage / audit plane" — the engine
  strip lists exactly 14 names (claude, omp, codex, dsh, opencode, gemini, copilot, cursor, crush,
  goose, qwen, pi, hermes, mock) and the typed-error claim (unknown→400, unavailable→409) appears
  in the band. Green audit-plane box not mentioned in this caption but unambiguous. Agrees.
- **F2** (p.15): amber CP tags, 5a∥5b parallel pair, dashed amber repair loop labelled
  "repair: best-of-3, global ≤5", green hard-gate boxes (7/8.5/9), right-to-left deliverables row,
  bottom deliverables strip (report.md / diagnostic-report.html / run_summary.json). All seven
  caption sentences verified against the drawing. Agrees.
- **F3** (p.37): caption colour words match segments; "printed fraction is Top-1-correct" explains
  the 0/2, 5/6, 1/1 labels; controls excluded from bars as stated. Agrees (see §5 D4 for a minor
  readability nit).
- **F4** (p.39): every bar labelled with verdict + value; caption's 0.65/0.58/0.60 all appear on
  the bars; hatch/grey/amber wording matches. Agrees.
- **F5** (p.40): every annotation visible in the panel (window rows 631–919, 210 switches, 125
  rows/106 runs/mean 1.18, pump 0.947 vs 0.990 A −4.3%) and in the evidence strip (30.62 vs
  31.73 L/min, −3.5%, 39.4% residence, capped COMPETING_SET 0.65, named missing channel).
  Caption sentence "Every annotation is read from the released run artifacts" is literally true
  (verified against artifacts, §4). Agrees.
- **F6** (p.45): cell colours and all 24 cells match the caption's colour key; IDD column shows
  5×Top-1 + 1 capped CS = "5/6". Agrees. Table A1 (p.71) is cell-for-cell identical to F6.
- **F7** (p.46): grey/blue/green grouping as captioned; all six rows print fraction + CI; caption's
  "no significance test is claimed" matches the figure's neutral design. Agrees.
- **F8** (p.55): six columns as captioned; teal sequential PCA ramp (cols 4–5) clearly does not
  reuse the semantic hues; legend has all six swatches incl. hatched sky. Agrees (one wording nit,
  §5 D3).
- **Tables**: T1, T3, T4, T5, T6, T7, T8, T9, A1, A2 all render inside the text block with
  booktabs rules intact, no column collisions, footnotesize/scriptsize text legible. T7's
  per-scenario signature column matches F4/F5/F8 values (0.65/0.58/0.60/0.79/0.75/0.82/0.80/0.89;
  85/100 rubric incl. −15 SKAB and −10 IDV11). T9 ΔConf −0.09/+0.07/0.00 matches the "±0.07–0.09"
  text. T5 totals (2+6+1 faults, 3 controls) match F3a's per-dataset bars.

## 3. Legibility, colour-blind and grayscale robustness

- Nominal = printed font sizes by construction (PRINT_W = 5.4 in = measured \textwidth 5.38 in);
  smallest text: F8 y-labels 7 pt, F4 bar labels 7.2 pt, F5 evidence strip 8 pt. All ≥6.5 pt
  effective. The 110 dpi page renders remain readable after 3× zoom; vector sources are
  fonttype-42 (embedded, editable) per `make_figures.py:34`.
- Hatch visibility: black '///' hatch (hatch_linewidth 0.5) on capped-CS sky bars/cells is clearly
  visible in F3a, F4 and F8 renders at print size; it is what keeps ranked-CS separable from solid
  blue in grayscale (sky+black-hatch min luminance 21 vs solid blue mean 129 in the p-55 render).
- Grayscale caveat (measured, not blocking): Okabe-Ito blue vs green luminance is nearly identical
  (rendered cell means 129.3 vs 129.9 on p-55). No information is lost because every blue/green
  contrast is redundant with printed text ("Top-1" vs "normal", "hit" vs "correct", labelled
  forest rows). Recorded as a design note; no action required.
- F4/F8 amber always carries dark text (near-black `#3a2600`/`AMBER_DARK`), legible on the
  saturated fill in both zooms.

## 4. Figure–text–repository spot checks (numbers)

Figure↔text↔ground-truth checks (11 performed; all match):

1. Controls CI: F7 "[43.8, 100.0]" = Table 6 (`main.tex:446`) = recomputed wilson(3,3,1.96);
   `paper/figures/make_figures.py:74` z=1.96 = `scripts/benchmark/aggregate.mjs:28` — **match**.
2. Headline Top-1: F7 6/9 [35.4,87.9] = Table 6 = abstract (`main.tex:55`) = `results/benchmark/
   metrics.json` (top1=6, fault_cases=9) — **match**.
3. Top-k: F7 8/9 [56.5,98.0] = Table 6 = metrics.json topk=8 — **match**.
4. All 12 confidence values in F4/F8/Table 7 = `results/benchmark/gradings/*.json`
   (65/58/78/88/88/79/60/75/82/80/89); the ranked/unranked split (hatched sky vs amber) matches
   the released `topk` flags exactly (cavitation True→hatched; tep_d03 False→amber) — **match**.
5. F5 annotations = canonical run artifacts
   (`workspace/diagnostic-runs/202609150453290_bench_skab_valve1_1/02_processed/
   anomaly_segment_analysis.json`): window [631,919]; switch_count_30_31=210; 30.0-count=125;
   n_runs_at_30=106; mean_run_len=1.18; current 0.947 vs 0.9898 A (−4.3%); "39.4% residence at
   lowest quantized level vs 0.0% outside"; confidence.json 65→0.65 — **match** (7/7 values).
6. F6 = Table A1 cell-for-cell; text p.45 "IDD 4/4; GPT-4o 3/4; o1-preview 4/4" matches the
   rendered cells (IDV1 wrong / IDV7,11,14 correct) — **match**.
7. F3a fractions 0/2 + 5/6 + 1/1 = 6/9, consistent with T5 dataset counts and F7 — **match**.
8. F8 PCA cells IDV3 "3"/"5" and IDV4 "49"/"100" vs text/Table 8 "2.9%/4.8%" and "SPE 100% vs
   T² 48.8%" — integer rounding (`%.0f`) consistent (48.8→49) — **match**.
9. Rubric deductions: T6/T7 caption −15 (SKAB) / −10 (IDV11) = gradings rubric scores 85/90 —
   **match**.
10. T9 IDV14 R2 = det 0.89 = canonical F4/F6/F8 values; IDV1 R2 = 0.79 = canonical — **match**.
11. F1 "fourteen engines" and 400/409 typed errors = engine strip count (14) = repository
    ground truth (HARNESS_DEFS, typed errors 400/409) — **match**.

## 5. Defects

Blocking: **none found.** The figure programme is content-correct, convention-consistent, and
reproducible from released data (`make_figures.py` reads metrics/gradings/cases/run artifacts
directly; re-running regenerates all six result figures).

Should-fix / cosmetic:

- **D1 (cosmetic, LaTeX-flagged)** — Table 2 (p.17, `tab:modules`): "Float too large for page by
  15.19078pt" (`main.log:1008`; table ends at `main.tex:197`). Measured on the render: the
  \bottomrule sits at y=1049 px vs the folio at y=1060 px — ~7 pt clearance; the table's last rows
  intrude into the bottom margin. No collision or content loss, but production will bounce it.
  Fix: tighten `\tabcolsep`/row separation, or move the caption's final sentence ("runs
  additionally hold auxiliary…") into an unnumbered table note, or `\resizebox` as T9 does.
- **D2 (cosmetic)** — p.54 is a 5-line section-end stub: the tail of the §8.5 case-study
  paragraph sits alone above ~85% whitespace because `placeins [section]` defers Fig 8 to p.55
  (float source order at `main.tex:587`, Section 9 starts `main.tex:623`). Acceptable in a
  preprint review build and the top-alignment/\@fptop=0pt discipline otherwise holds on every
  float page; but the stub is avoidable — move the `fig:matrix` figure environment one paragraph
  earlier (before the "Three findings emerge" block) so the stub text rejoins the float page, or
  let Section 9 start on p.55 beneath the float. Judged acceptable for review; repair before
  production.
- **D3 (trivial)** — F8 caption punctuation drift: cells print a single hyphen "-" where the
  caption writes ``--''; the control cell prints "no (correct: control)" (two lines), slightly
  more informative than the caption's grey ``no''. Align the caption strings with the rendered
  cells.
- **D4 (minor suggestion)** — F3a prints "0/2" beside a stacked bar of visible length 2 (SKAB).
  The caption explains the fraction is Top-1-only, but the first visual read is jarring. Consider
  printing "0/2 (+2 top-k)" or annotating the hatched segment. Not required.
- **D5 (record only)** — blue/green near-identical grayscale luminance (§3); safe today because
  text always disambiguates. If a future figure drops per-cell labels, revisit.

## 6. Bottom line

The figure programme is print-clean for AEI in content, colour semantics, caption agreement and
reproducibility: all eight assigned convention requirements pass as rendered, every figure number
I traced (11 checks) reproduces from the released repository artifacts, and no figure relies on
hue alone. Two typesetting repairs (Table 2's 15.19 pt depth overflow; the p-54 five-line stub)
should be made before production but touch no content.

PUBLISH-READY for the figure programme after those two quick float-layout fixes.
