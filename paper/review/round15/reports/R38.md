# R38 — Industrial-Informatics Systems Review (Round 15, blind)

Reviewer scope: §2–§3 architecture claims (Table 2 caption + float fit; RAG two-mechanism
split; package census; VLM nested dispatch; engine realization 12+2; CP-1..CP-9; repair
governance; model assignment), judged against the repository and **canonical graded runs
only** (run_dir recorded in each `results/benchmark/gradings/*.json`). Blindness rule
honored: nothing under `paper/review/` read except the briefing and page PNGs.

## Canonical run map used (from grading records)

| Scenario | Canonical run_dir |
|---|---|
| skab_valve1_1 | 202609150453290_bench_skab_valve1_1 |
| skab_cavitation_13 | 202609141843516_bench_skab_cavitation_13 |
| skab_normal_control | 202609141844090_bench_skab_normal_control |
| tep_d00_normal_control | 202609141846394_bench_tep_d00_normal_control |
| tep_d01_ac_feed_ratio | 202609150454126_bench_tep_d01_ac_feed_ratio |
| tep_d03_hard | 202609141846195_bench_tep_d03_hard |
| tep_d04_reactor_cooling_step | 202609141854259_bench_tep_d04_reactor_cooling_step |
| tep_d07_header_pressure | 202609141854457_bench_tep_d07_header_pressure |
| tep_d11_reactor_cooling_random | 202609141855230_bench_tep_d11_reactor_cooling_random |
| tep_d14_reactor_valve_sticking | 202609150454479_bench_tep_d14_reactor_valve_sticking |
| indpensim_batch093 | 202609141847046_bench_indpensim_batch093 |
| indpensim_batch001_control | 202609141850296_bench_indpensim_batch001_control |

Timestamps corroborate the paper's execution-window claim (§5 C1(iii)): nine runs
2026-09-14 18:43–18:55 (+8) and three runs 2026-09-15 04:53–04:54 (+8).

## Spot checks (all judged against canonical graded runs)

**SC1 — Package census (18 / 12 / 8 / 6). MATCH.**
`ls .claude/skills/` = 18 packages; `ls .claude/skills/*/references/agent-protocol.md | wc -l` = 12.
The 8 dispatched packages (ontology-builder, data-processor, diagnostician, judge,
physical-auditor, reporter, html-visualizer, html-reviewer) all carry the protocol file.
The 6 without are exactly the paper's enumeration (§3.1, p.10): orchestrator
(industrial-analysis-auto), Step-0.5 pre-processor (industrial-data-preprocessor),
enhancement (industrial-analysis-enhance-auto), RAG knowledge building
(rag-knowledge-builder), dashboard rendering (diagnostic-html-visualizer), skill fitness
(darwin-skill). "Most also ship the JSON Schemas and validation scripts" also holds
(6 of 8 dispatched packages ship schemas; data-processor has 5 schemas / 33 scripts;
physical-auditor and html-visualizer ship none — "most" is accurate).

**SC2 — Engine realization "12+2" and typed errors. MATCH.**
`app/backend/src/harness/engines.mjs:21` `HARNESS_DEFS` has exactly 12 entries (mock, codex,
dsh, opencode, gemini, copilot, cursor, crush, goose, qwen, pi, hermes);
`app/backend/src/services/diagnosis.service.mjs:45` `ENGINES` adds exactly the two
first-party wiring entries (claude, omp) = 14. `availability.mjs:16–31` defines
`HARNESS_UNKNOWN` (status 400) and `HARNESS_UNAVAILABLE` (status 409 with resolved-command
hint); `diagnosis.service.mjs:99` wires `assertHarnessUsable` into the start path, and the
module header states probes share the binary resolution of the real spawn. The paper's
§3.4 sentence "realized as a twelve-entry capability table (engines.mjs) plus the two
first-party wiring entries in the service dispatch table" is literally true of the code.

**SC3 — VLM nested dispatch and the "3+3.3" label. MATCH.**
Orchestrator dispatch table: `.claude/skills/industrial-analysis-auto/SKILL.md:57` and
`:82` label the sub-step `3+3.3` mapped to industrial-data-processor (VLM included).
The VLM is dispatched by the data-processor, not the orchestrator:
`.claude/skills/industrial-data-processor/SKILL.md:83` ("VLM Visual Analysis Dispatch
(Phase 5.5)") and `:136` contain a literal `Agent({ subagent_type: "vlm-visual-analyzer", ... })`
nested call. Canonical-run corroboration: tep_d01's
`03_figures/visual_analysis.json` `analysis_provenance.source_agent` = "data-processor（协议
5.5.6 无 VLM 运行内联收尾 …）". The briefing's note that the 3+3.3 label lives in the
SKILL.md dispatch table (not run event logs) is consistent with what the event logs contain.

**SC4 — CP-1..CP-9 predicates (Table 3). MATCH, 9/9 rows.**
Orchestrator `SKILL.md` checkpoint quick-reference (lines 438–392): CP-1 three input files →
back to Step 0; CP-2 ontology.json ≥1KB + schema-valid; CP-3 clarification_status ∈
{AUTO_RESOLVED, USER_CONFIRMED}; CP-4 data_analysis_conclusion.json + plots>0 (line 334:
plot_manifest.json plots > 0); CP-5 four diagnosis outputs schema-valid + quality check;
CP-6 judge_repair_summary.json + pre-audit no FATAL (line 443); CP-7 report.md +
run_summary.json; CP-8 optimizer.md contains ENDORSED; CP-9 diagnostic-report.html ≥5120B +
html_review.json verdict=pass (line 392). Table 3 (p.18 as rendered) is a faithful
transcription, including the failure actions. The 5120 B HTML floor is also enforced
grader-side (`scripts/benchmark/zcode_direct_pipeline.mjs`, `pipelineMissing`).

**SC5 — RAG two-mechanism split across the 12 canonical runs. MATCH, every branch.**
- Successful retrieval: canonical `skab_valve1_1/00_input/rag_deep_understanding.json`
  `retrieval_meta.chunks_retrieved = 21` — the paper's "pulled 21 chunks".
- Explicit fallback events: canonical tep_d01 (202609150454126) `.pipeline_events.jsonl`
  records `fallback_directive` + `unavailable_directive_fallback`; canonical tep_d00
  (202609141846394) records `fallback_parameter_to_physics`; canonical batch001/batch093
  event logs record fallback events referencing `rag_deep_understanding`. This is exactly
  the paper's §3.2 sentence (p.11–12), including that the canonical (09-15) tep_d01 run —
  not the superseded 09-14 run — is the one bearing the events.
- Artifact-level degradation: canonical tep_d14 `rag_deep_understanding.json` has
  `rag_available = false` with `fallback_reason` = rag-knowledge-builder unavailable +
  `/retrieve` returned no TEP-domain knowledge → "used L5 standard TEP benchmark semantics
  + L4 data self-description" — matching the paper's L5/L4 fallback wording verbatim.
  batch001's artifact records the "3s health probe failed" reason (probe verified below).
- Silent fast path: canonical tep_d03/d04/d07/d11 and skab_cavitation/skab_normal logs
  contain `fastpath`/`store reuse` step events and **zero** RAG fallback events; the two
  SKAB runs have no RAG artifact at all. The paper's "no fallback event in their logs" is
  precisely scoped and true.
- Probe: `industrial-analysis-auto/SKILL.md:225–227` — `curl -m 3` health check at Step 0.

**SC6 — Unique L5/L6 corroboration-only item. MATCH.**
Sweeping `04_diagnostics/evidence*.json` across all 12 canonical runs: the combined
marker `L5/L6` occurs exactly once in the corpus — canonical
`tep_d14/04_diagnostics/evidence.json`, `evidence_inventory.physical_evidence[3]`:
source "TEP基准过程知识(L5/L6)", implication "先验一致性(仅作旁证，不作判据)" =
corroboration-only, not a criterion. Count in the other 11 canonical runs: 0.

**SC7 — Repair governance (≤3 per fault, ≤5 global, anti-oscillation ≤0.50). MATCH.**
`industrial-analysis-auto/SKILL.md`: repair loop "Best-of-3" with `best_score`/`best_round`
snapshot (lines ~352–362); global re-diagnosis cap 5 "tracked by `repair_spawn` in
`.pipeline_events.jsonl`" (line 471); anti-oscillation: 3rd same-issue oscillation →
`COMPETING_SET`, confidence ≤50 (lines 370, 473). Diagnostician-side ceilings
`industrial-diagnostician/SKILL.md:106`: INDISTINGUISHABLE ≤65, COMPETING_SET ≤70,
[PARAM_AMBIGUITY] ≤50; no DETERMINED cap in the skill (paper states this correctly and
attributes the 0.90 DET ceiling to the machine gate). Canonical-run census: exactly **one**
`repair_spawn` event across all twelve canonical logs — batch001_control, iteration 2,
judge `repair_rounds: 1`, scope "Wt/V slope-vs-ratio numeric wording" — matching the
paper's "eleven scenarios required zero repairs; one control required a single repair
round" (§5.4) and §7.3's "one judge-repair round was spent sharpening exactly this
attribution". (Earlier grep hits in tep_d01/d11 logs are `files_written` field names
(`judge_repair_summary.json`), not repair events.)

**SC8 — Model assignment. MATCH.**
`.omp/agents/` contains exactly 14 agent definitions. Only `vlm-visual-analyzer.md` carries
`model: vision`; 10 are `default`; the three non-default optional-layer agents
(deep-analyst, enhance-orchestrator, physics-bridge) are `sonnet`, not vision. The paper's
VLM-as-vision-agent framing and §5.4's disclosure that the vision stage executed its
non-visual degradation are consistent: all five canonical `visual_analysis.json` files I
opened record `observation_mode: metadata_backed_inference` with script provenance
(`visual_analysis.py` / data-processor inline finalization; `vlm_enabled: false` where
recorded).

**SC9 — Verdict/ceiling numbers from canonical gradings (Table 2-adjacent, §4.1). MATCH.**
Canonical tep_d01: `diagnosis.json` `diagnosis_type: DETERMINED`, `confidence.json`
five-factor breakdown 22/25 + 16/25 + 18/20 + 16/20 + 7/10 = 79 → paper's "det 0.79" and
the 25/25/20/20/10 weights, both verified from the released artifact; judge
`overall_score = 94` (within the stated 90–97 range). Canonical tep_d04: DETERMINED, 75,
with a reason-coded ceiling `{ceiling: 75, reason: DIRECT_MEASUREMENT}` — exactly the
paper's §4.1 example. Canonical skab_valve1_1: COMPETING_SET at 65 — Table 5's "cs 0.65".

**SC10 — Grader artifact set and finaliser inventory. MATCH.**
`zcode_direct_pipeline.mjs` `PIPELINE_ARTIFACTS` = exactly the 14 paths of Table 4, same
order; the function appends the <5120 B HTML rule. Canonical tep_d01
`pipeline_finalize_report.json`: `overall: PASS`, 33 inventory checks + 14 schema checks
(+ step summaries) ≈ the "≈45-check inventory" of Algorithm 1 line 10. Provenance labels
in `gradings/tep_d01_ac_feed_ratio.json`: `judge-agent-10-criteria`, `auditor-agent`,
`html-reviewer-agent` — exactly the three labels §5 C3 names.

## Table 2 caption and float-page fit (rendered-image judgment)

- Table 2 (p.17) is placed alone on a float page and **fits** (top-aligned per the
  `\@fptop` setting; ~20–25% white space below). The caption's honesty qualifier —
  "The table lists each stage's contracted deliverables; runs additionally hold auxiliary
  stage-authored files" — is verified against the canonical tep_d01 inventory, which holds
  every contracted file plus genuine auxiliaries (anomaly_report.json, batch_integrity.json,
  time_lag_analysis.json, physics_check.json, 06_scripts/*, judge_repair_summary.json).
  "Filenames as released; all paths relative to the run directory" holds.
- Table 3 (p.18) fits its float page; predicates identical to the orchestrator's
  quick-reference (SC4).
- Layout defect (cosmetic): both tables are first cited on p.14 but flush to float pages
  p.17/p.18, leaving p.16 ~45% empty and p.18 ~55% empty. The source preamble discloses
  this as best-effort preprint float behavior, but a reader still meets three consecutive
  under-full pages. Additionally, Table 2's caption breaks awkwardly: the line ends
  "Checkpoint predicates:" and "Table 3." sits orphaned on its own line.
- Figures in my scope, as rendered images: Fig 1 (p.13) — five planes, 14 engines listed,
  crisp and legible; contents match engines.mjs/HARNESS_DEFS. Fig 2 (p.15) — two-row flow
  with correct CP tags, green hard gates, dashed repair edge labeled "repair: best-of-3,
  global ≤ 5"; matches the code constants; print legibility OK at \textwidth.

## Findings

Must-fix (blocking): **none** — every architecture claim I checked in §2–§3 is true of the
released codebase and of the canonical graded runs; the paper's scoping clarifications
(8 dispatched agents, VLM nested call, optional layers) map 1:1 onto the repository.

Should-fix (cosmetic / precision):
1. §3 floats (Tables 2/3): repack nearer their p.14 citation or share a float page; the
   current build shows three consecutive under-full pages (p.16–p.18).
2. Table 2 caption: fix the orphaned "Table 3." line break.
3. Table 2 Stage column hyphenates "Visual evi-dence"; widen slightly or allow the row
   label to break as "Visual evidence (3.5)" without a hyphen.
4. §3.1 "while the remainder form optional layers" — the remainder strictly includes the
   orchestrator package (not an optional layer) and the Step-0.5 pre-processor (invoked as
   a script inside the executed path); one clarifying clause would preempt a careful
   reader's objection. (The second scoping clarification already names both correctly, so
   this is wording, not accuracy.)

## Verdict rationale

The §2–§3 system description is the most code-faithful system section I have reviewed:
counts, filenames, predicates, error codes, event names, ceiling constants, and per-run
RAG behavior are all literally verifiable and verified, including the honesty qualifiers
(auxiliary files, script-signed visual analysis, corroboration-only L5/L6 item). The only
defects I can charge are float-page placement and two caption/wording blemishes.
