# R21 — Empirical Methods & Statistics Review (Round 11, blind)

Reviewer role: EMPIRICAL METHODS & STATISTICS (defensible design, correct uncertainty
quantification, fair baselines, honest disclosure). Reviewed `paper/main.tex` (742 lines,
73 compiled pages), rendered pages `paper/review/round11/pages/p-*.png` for all figure/table
pages in my focus areas, and the repository ground truth under `results/benchmark/`,
`scripts/benchmark/`, `workspace/diagnostic-runs/`, `.claude/skills/`, and git history.
Blindness rule respected: nothing under `paper/review/` other than the briefing was read.

---

## 1. Abstract audit (≤300 words, qualifier loss, number fidelity)

- Word count: **256 words** (LaTeX stripped, `SECTIONREF` counted as one word). Comfortably ≤300.
- Every number in the abstract was re-derived from artifacts:
  - 12 scenarios / 3 datasets / 9 faults / 6 TEP / 3 controls → `results/benchmark/metrics.json`
    (`total_cases:12, fault_cases:9, control_cases:3`) — match.
  - 6/9, Top-1 66.7%, Wilson 95% CI [35.4%, 87.9%] → `metrics.json` `cdr:0.6667`,
    `cdr_ci95:[35.42, 87.94]`; independently recomputed Wilson = [35.4, 87.9] — match.
  - "5/9 under the pre-revision keyword set" — present in abstract; verified by my own
    re-implementation of the grader matching logic (§4 below) and by
    `results/benchmark/keyword_sensitivity.json` — match.
  - "ranks the true mechanism among the candidates in two more" (= top-k 8/9, since IDV3
    excludes the true mechanism) → `metrics.json topk:8`; gradings: valve topk=true,
    cavitation topk=true, tep_d03 topk=false — match.
  - Three capped COMPETING_SET verdicts; controls 3/3 zero false alarms (`control_pass:3,
    false_alarms:0`) — match.
  - TEP 5/6 (4/6 pre-revision) — match (`by_dataset.tep.top1:5` of 6 faults; sensitivity file
    `summary.top1 pre 5/9 vs cal 6/9` with tep_d01 the single Top-1 flip).
  - Bare single call resolves 9/9 — match (`baselines.json summary:
    "TEP 6/6, non-TEP 3/3 (all 9/9 faults hit)"`).
- **No qualifier was lost in compression**: both pre-revision qualifiers ("5/9 under the
  pre-revision keyword set", "4/6 under the pre-revision keyword set") survive in the
  abstract (main.tex line 55), as do "deliberately capped" and the ablation bounding clause
  ("more than the pipeline"). Rendered page p-01 confirms clean typesetting.

## 2. Headline-qualifier placement (6/9→5/9 and TEP 5/6→4/6)

Occurrences in main.tex: lines 55 (abstract, both), 81 (contribution 4, both), 335 (§5 C1,
5/9; TEP flip named as IDV1), 423 (§7.1 prose, 5/9), 521 (§8.1 prose, 4/6), 534 (Fig. 6
caption, 4/6), 649 (conclusion, both), 673 (Table A1 caption, 5/6 calibrated vs 4/6
pre-revision, with the mechanism of the flip). Gaps:

- Table 6 (tab:overall, line 435, rendered p-35) prints "Resolved Top-1 … 6/9 = 66.7%" with
  no pre-revision qualifier in the row or caption (the qualifier sits in the preceding prose).
- Fig. 7 caption (line 541, rendered p-46) prints "the TEP subset (5/6), all nine faults
  (6/9)" without the qualifier.

The qualifier is present at every prose surface; the two number-bearing surfaces above are
the exceptions. Should-fix (one parenthetical each).

## 3. §4 two-layer caps precision — verified exactly

- Agent-side: `.claude/skills/industrial-diagnostician/SKILL.md:106` —
  "Confidence ceilings: INDISTINGUISHABLE ≤ 65, COMPETING_SET ≤ 70, [PARAM_AMBIGUITY] ≤ 50";
  no DETERMINED cap anywhere in the skill (grep across package) — matches the paper's claim
  that 0.65/0.50 are agent-documented and no DET cap is agent-documented.
- Machine gate: `scripts/benchmark/judge-rubric.mjs` (R4, ~line 133):
  `const ceiling = D.diagnosis_type === 'COMPETING_SET' ? 0.70 : 0.90;` — matches the paper's
  "machine grading gate enforces ≤0.90 for DETERMINED and ≤0.70 for CS" and "the machine gate
  applies the flat 0.70 CS ceiling" (R4 does not special-case INDISTINGUISHABLE).
- Reason-coded ceilings: `.claude/skills/industrial-diagnostician/schemas/confidence_schema.json:121`
  enumerates exactly the five reasons the paper lists
  (INDISTINGUISHABLE_COMPETING_SET, UNSORTED_DATA_LAG_CLAIMS, PRODUCT_SPECIFIC_ONLY,
  ALL_CRITERIA_VERIFIED, DIRECT_MEASUREMENT) — match.
- Worked example: canonical IDV4 run `202609141854259_bench_tep_d04_reactor_cooling_step/
  04_diagnostics/confidence.json` → `confidence_ceilings_applied: [{hypothesis_id:"H1",
  ceiling:75, reason:"DIRECT_MEASUREMENT"}]`, five-factor sum 22+22+18+18+8=88 → reported 75
  = min(F, reason cap) — matches §4/§7.3 precisely.
- Elimination contract: `schemas/diagnosis_schema.json` (~line 340)
  `"exclusion_confidence": {"minimum": 90, "maximum": 100}` — matches "schema floor ≥0.90".
- Five-factor weights: `diagnostic-quality-check.mjs` (~line 151)
  `factorMax = {statistical_strength:25, physical_plausibility:25, temporal_evidence:20,
  absence_of_confounds:20, symptom_completeness:10}` — matches 25/25/20/20/10.

## 4. §5 C1 timeline and keyword sensitivity — verified, one precision defect

Verified against `results/benchmark/journal.jsonl` (104 entries), run-directory names,
grading files, and git:

- Switch journaled 04:06–04:09 (+0800) 09-15: batches `2026-09-14T20:06:32/33Z` and
  `20:09:48/49Z` (= 04:06:32–04:09:49 +0800). The 04:06:33 entry grades tep_d01 with
  `top1:false` (old set) and the 04:09:48 entry grades it `top1:true` (new set) — the
  "old-set grading superseded" claim is exact. Match.
- Twelve canonical executions span 09-14 18:43 → 09-15 04:54 (+0800): released
  `results/benchmark/gradings/*.json` point at nine run dirs `20260914 1843516–1855230`
  plus three `20260915 0453290/0454126/0454479` — i.e., nine evening executions and three at
  04:53–04:54. Match.
- Commit `221d061` = 2026-09-15 19:20:18 +0800 (git log) — matches "committed the same day
  (commit 221d061, 19:20)". The committed `benchmark_cases.json` diff (221d061~1 → 221d061)
  is exactly the described calibration: generic single-char tokens removed (入口/温度/压力/
  水/波动), mechanism synonyms added (配比 etc.), and the in-file revision note states a
  one-shot uniform application "非按结果逐例调整". Match.
- Released gradings written 2026-09-16 17:43 +0800 (file mtimes = last journal batch
  09:43:20Z), i.e. all released gradings post-switch. Match.
- **Defect — "every canonical grading was computed after the switch, the first at 06:22"
  does not reproduce as written.** (a) The 04:06:32–33 +0800 batch (pre-switch keyword set)
  also graded three *canonical* run dirs (skab_cavitation_13, skab_normal_control,
  tep_d00_normal_control); their outcomes are identical under both keyword sets
  (`keyword_sensitivity.json`: changed=false), so no result is affected, but "every canonical
  grading … after the switch" is literally false. (b) The earliest post-switch batch grading
  canonical run dirs is `2026-09-14T21:17:16Z` = **05:17:16 +0800** (tep_d03/d04/d07/d11,
  calibrated results identical to the released files); batch `22:29:11Z` = 06:29 +0800 adds
  batch093/batch001. (c) "06:22" matches only the *UTC* journal stamp `2026-09-15T06:22:48Z`
  (= 14:22 +0800) of the batch that graded the three 09-15 canonical runs — a timezone mix
  inside a sentence whose other timestamps are explicitly UTC+8. Materially inconsequential
  (no outcome depends on any pre-switch grading), but this sentence is the paper's own
  provenance exhibit and must reproduce exactly.
- Sensitivity recompute (independent re-implementation of the grader matching at
  `scripts/benchmark/zcode_direct_pipeline.mjs:366–390`: flatten primary_finding + surviving
  hypotheses' name/physical_logic_chain/supporting_evidence/ontology_data_physics_proof,
  lowercase substring match): on the canonical diagnosis.json files —
  tep_d03_hard: pre-revision hits = ["温度"] (spurious, the deleted generic token) →
  topk=true; calibrated hits = [] → topk=false. tep_d01_ac_feed_ratio (DETERMINED):
  pre-revision hits = [] → top1=false; calibrated hits = ["配比"] → top1=true. This
  reproduces `keyword_sensitivity.json` exactly (Top-1 5/9→6/9, top-k 8/9 both, with the
  tep_d03 top-k loss and tep_d01 top-k gain netting to 8/9). Match.

## 5. Wilson 95% intervals — seven recomputed, all correct

z = 1.959964; all agree with the paper to 0.1 pp:

| Claim | Paper | Recomputed |
|---|---|---|
| Top-1 6/9 | 66.7% [35.4, 87.9] | 66.7% [35.4, 87.9] |
| top-k 8/9 | 88.9% [56.5, 98.0] | 88.9% [56.5, 98.0] |
| DET precision 6/6 | [61.0, 100.0] | [61.0, 100.0] |
| Controls 3/3 | [43.9, 100.0] | [43.9, 100.0] |
| TEP subset 5/6 | 83.3% [43.6, 97.0] | 83.3% [43.6, 97.0] |
| FE GPT-4o 7/11 | 63.6% [35.4, 84.8] | 63.6% [35.4, 84.8] |
| FE o1-preview 9/11 | 81.8% [52.3, 94.9] | 81.8% [52.3, 94.9] |

Pre-revision counts for reference: 5/9 → [26.7, 81.1]; 4/6 → [30.0, 90.3] (not quoted in the
paper; no error involved). The n=9 power limitation, the single-execution caveat, and the
repair-loop selection effect are disclosed (§8, first paragraph) with the correct counts
(eleven zero-repair scenarios; exactly one `repair_spawn` event exists in the canonical
corpus, in `indpensim_batch001_control`). The non-independence of the determined-precision
row is flagged in the Table 6 caption. No significance test is claimed across systems.

## 6. §7–§8 stability / determinism / FE fairness / ablation honesty

- **Rubric**: `rubric.json` scores = [85, 100×9 …, 90, 100] → ten 100s, one 90, one 85,
  mean 97.92 → paper's "97.9, ten at 100, one at 90, one at 85" — match, with the honest
  "limited discriminative power" scope statement in §6.2.
- **Judge gate**: per-case scores mean 93.08, range 90–97 → "93.1 (range 90–97)" — match.
- **Determinism**: `suite_determinism.json` — compared 45, identical 45, verdict
  DETERMINISTIC, method "only executed_at is excluded" → paper's "45 output files
  byte-for-byte with timestamps excluded" — match.
- **Stability**: `stability_report.json` — `era_case_pairs:15, consistent:15`;
  my per-era recount with `min_runs_required:2` gives v1 = 12 pairs, v2 = 3 pairs
  (tep_d01, tep_d14, skab_valve1_1) → the paper's "15/15 case-version pairs … pre-discipline
  era 12/12; current era 3/3" is exact, including its definition sentence. The three
  sensitive scenarios (valve, cavitation, tep_d03) indeed flip v1 DETERMINED → v2
  COMPETING_SET (era boundary 20260914) — the "documented tightening, not run-to-run
  instability" framing is supported. The direct 3-scenario check reproduces exactly:
  0.88→0.79 (Δ−0.09), 0.82→0.89 (Δ+0.07), 0.65→0.65 (Δ0.00), all six runs read from
  `04_diagnostics/confidence.json`. The IDV14 period-estimate divergence (≈12 min vs ≈30 min)
  is disclosed as a signature-level instability rather than hidden.
- **Seeded re-test draw**: `retest_selection.json` — 4 rounds; rounds 2–4 all selected
  tep_d11_reactor_cooling_random; round 4 seed 149290398, u=0.803782468 — matches the paper
  to the last digit, including the disclosure that tep_d11 has only one proven v2 run
  (`stability_report` v2 n_runs_proven=1) and is recorded INSUFFICIENT-RUNS with an
  outstanding execution contract. Honest.
- **FE fairness**: the paper states FE's protocol differences wherever Register A is invoked
  (top-3 + alias vs strict single verdict; candidates in prompt vs none; different base
  models; sets differ so no test). Its own FE-protocol replication on the same model scores
  6/6 — above IDD's 5/6 TEP — and is reported as such; the FE-official-code arm scores 5/6
  with the miss on tep_d04 (`baselines.json fe_official_code.per_fault.tep_d04.strict_hit:
  false`, top3 = IDV1/IDV2/IDV8), matching the paper's explanation ("T² dominated by
  feed-composition shifts"). The bare-LLM arm's control coverage gap ("2/2, not every control
  was run", TEP control not run bare) is disclosed in text and visible as "n/a" in Fig. 8.
- **Ablation honesty**: the 9/9-bare vs 6/9-pipeline result is stated in the abstract,
  §1, §8.3, and the conclusion, and is used to bound the accuracy claim rather than buried.
- **Fingerprints/consistency**: `repro_report.json` — dataset_integrity 153/153 verified,
  12/12 coverage, recomputed metrics drift-free, status REPRODUCIBLE — matches C2/C5.

## 7. Figures as rendered (statistics-relevant)

- **Fig. 3** (p-36): (a) dataset bars 0/2 + 5/6 + 1/1 = 6/9, amber IDV3 miss; (b) rubric vs
  judge per scenario consistent with `rubric.json`/`metrics.json` (85 SKAB valve, 90 IDV11,
  controls grey ≥90). Clean.
- **Fig. 4** (p-38): all bar values match Table 7 and the artifacts (0.65/0.58/0.60 capped;
  0.75–0.89 DET; controls 0.78/0.88/0.88); the 0.70 line is annotated "ceiling for capped
  verdicts only", correctly avoiding the implication that DETERMINED is capped at 0.70.
  Axis label carries "overconfidence = DETERMINED and wrong: 0 occurrences". Clean.
- **Fig. 5** (p-39): window 631–919, 210 switches, 39.4% floor dwell, 0.990→0.947 A (−4.3%),
  capped CS 0.65 — all read back from
  `202609150453290_bench_skab_valve1_1/02_processed/data_analysis_conclusion.json`
  (lines 325/348) and `diagnosis.json`. Clean. (Minor: the figure's "125 rows" ≤30.5 L/min
  and the 39.4% bin-30 dwell are two different floor-bin bookkeepings; see should-fix 3.)
- **Fig. 6** (p-45): per-fault matrix matches Table A1 and §8.1 cell by cell; caption carries
  the 4/6 pre-revision qualifier. Clean.
- **Fig. 7** (p-46): forest plot point estimates and intervals match my Wilson recomputation
  exactly (all seven rows). Clean.
- **Fig. 8** (p-51): column 1 confidences match released gradings (verified at vector
  resolution: TEP fault-free control reads 0.88, consistent with Table 7's 0.78–0.88 range);
  PCA T²/SPE cells match `report.md` (IDV3 3/5, IDV4 48/100, d11 54/66, batch093 15/74);
  column 2 shows all-faults "hit" with the TEP control honestly "n/a" (not run). Clean.

## 8. Spot-check ledger (claim → ground truth → verdict)

1. Top-1 6/9, 66.7%, CI [35.4, 87.9] → `results/benchmark/metrics.json` (`cdr:0.6667`,
   `cdr_ci95`) + independent Wilson recompute → **match**.
2. Two-layer caps → `SKILL.md:106` (65/70/50, no DET cap) vs `judge-rubric.mjs` R4
   (`ceiling = CS ? 0.70 : 0.90`) → **match** (paper describes both layers correctly).
3. IDV4 DIRECT_MEASUREMENT cap 0.75 → `202609141854259_…/04_diagnostics/confidence.json`
   (`confidence_ceilings_applied[0] = {ceiling:75, reason:"DIRECT_MEASUREMENT"}`) → **match**.
4. Keyword calibration → `git diff 221d061~1 221d061 -- scripts/benchmark/cases/benchmark_cases.json`
   (配比 added to tep_d01; 温度 removed from tep_d03; uniform-application note) + my
   re-implementation of the grader matching on canonical diagnosis.json → **match**
   (5/9→6/9; top-k 8/9 both).
5. C1 timeline "every canonical grading after the switch, first at 06:22" →
   `journal.jsonl` batches 20:06:32Z/20:09:48Z (switch), 21:17:16Z (=05:17 +0800, first
   post-switch canonical gradings), 06:22:48Z (=14:22 +0800, three 09-15 runs), plus the
   04:06:32 +0800 batch grading three canonical dirs pre-switch → **partial mismatch**
   (precision only; no outcome affected).
6. Determinism 45/45 → `suite_determinism.json` (`compared:45, identical:45,
   verdict:"DETERMINISTIC"`) → **match**.
7. Stability 15/15 = 12 v1 + 3 v2 → `stability_report.json` + per-era recount → **match**.
8. Baselines 9/9 bare, 6/6 FE-protocol, 5/6 FE-official (miss IDV4), controls 2/2 →
   `baselines.json` summary + `fe_official_code.per_fault` → **match**.
9. Commit timestamp → `git log 221d061` = 2026-09-15 19:20:18 +0800 → **match**.
10. agent-protocol.md in 12 of 18 packages → `ls .claude/skills/*/references/agent-protocol.md
    | wc -l` = 12 of 18 → **match**.
11. 153/153 fingerprints → `repro_report.json` `dataset_integrity {entries:153,
    verified:153, mismatched:0}` → **match**.
12. Seeded draw round 4 (seed 149290398, u=0.803782468, tep_d11; 3 of 4 draws tep_d11) →
    `retest_selection.json` → **match**.

## 9. Judgment

The empirical apparatus is unusually verifiable and almost everything I recomputed — every
headline number, all seven Wilson intervals, the sensitivity flip in both directions, the
caps at both layers, the determinism/stability/draw records — reproduces exactly, and the
paper repeatedly volunteers the facts that weaken its own accuracy story (bare model 9/9,
rubric at ceiling, ceiling-compliance circularity, single-execution evidence, FE-protocol
replication beating IDD on TEP). The one defect my verification found is a single C1
sentence whose timeline does not reproduce as written (mixed UTC/UTC+8 and a literally
overbroad "every canonical grading"), plus two headline surfaces missing the pre-revision
qualifier. These are wording-level fixes; nothing numeric changes.

**Verdict: MINOR_REVISION.**
