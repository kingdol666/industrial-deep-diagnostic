# Round 6 — AEI Blind Review Briefing (fresh reviewers)

You are an **Advanced Engineering Informatics (AEI)** reviewer. Your decision standard: is this paper
publishable in AEI as-is (accept / minor / major)? Judge content, figures, layout, and — critically —
whether the paper's description of the system matches **what the system actually does**. Do NOT trust
the paper; verify against the repository ground truth listed below. You have not seen any earlier
review rounds; form your own opinion.

## What you are reviewing

- Paper source: `D:\codes\myskills\industrial-deep-diagnostic\paper\main.tex` (68 pp, compiled `paper/main.pdf`)
- Rendered pages (110 dpi PNG, one per page): `D:\codes\myskills\industrial-deep-diagnostic\paper\review\round6\pages\p-001.png` … `p-068.png`
- Figures (vector PDF sources): `paper/figures/fig_*.pdf`, generated by `paper/figures/make_figures.py`

## Repository ground truth (verify claims against these)

Project: an industrial deep-diagnosis system — 9-stage multi-agent pipeline over sensor/process data,
plus a machine-checkable benchmark with three baselines, all driven by one LLM deployment.

| Paper claim area | Ground truth to check |
|---|---|
| 9-stage pipeline, agents, checkpoints CP-1..CP-9, repair governance (≤3/fault, ≤5 global, anti-oscillation ≤0.50) | `.claude/skills/industrial-analysis-auto/SKILL.md` and `references/agent-protocol.md` |
| Skill set (18 skills), agent personas (14) | `.claude/skills/*/SKILL.md`, `.omp/agents/*.md` |
| 14 execution engines, typed errors 400/409 | `app/backend/src/harness/engines.mjs` (HARNESS_DEFS), `app/backend/src/services/diagnosis.service.mjs` (ENGINES dispatch table) |
| Three-state protocol (DETERMINED/COMPETING_SET/NEEDS_DATA), confidence ceilings (0.90/0.70/0.65), evidence levels L1–L7 | `.claude/skills/industrial-diagnostician/SKILL.md` |
| Five-factor confidence weights (25/20/20/20/10) | released `confidence.json` in a benchmark run dir + diagnostician skill |
| Benchmark protocol (4-step runbook, truth isolation, fingerprinting, gates) | `docs/benchmark/benchmark-pipeline-runbook.md`, `scripts/benchmark/run-benchmark-pipeline.mjs`, `scripts/benchmark/aggregate.mjs`, leakage sentinel in `scripts/benchmark/` |
| Baselines: classical PCA / FE-protocol replication / same-model bare LLM | `baselines/baseline-suite/server/utils/{pca,fe,llm}.mjs`, `baselines/baseline-suite/server/api/diagnose/[caseId].post.mjs`, `scripts/benchmark/baseline_pca.mjs`, `scripts/benchmark/baseline_llm.mjs` |
| Headline numbers (top-1 6/9, Wilson CI [35.4,87.9], rubric 97.9, judge 93.1, controls 3/3, FE 5/6 + 8/11, determinism 45/45, stability 15/15 within-era, seeded retest seed 149290398 → tep_d11) | `results/benchmark/*.json` (metrics, scoring, stability, suite_determinism), `results/benchmark/baseline_comparison_report.md` |
| Per-fault TEP comparison vs FaultExplainer | Appendix A tables vs FE-reported numbers in `results/benchmark/` |
| Case study (SKAB valve1: window [631,919], 210 switches, 39.4% floor dwell, −4.3% pump current) | the SKAB run artifacts under `results/benchmark/` (case study run dir), `paper/figures/make_figures.py` fig_case_study() |

Benchmark scenarios + ground-truth labels: `scripts/benchmark/cases/` (you may read these; the
diagnosis system itself was never allowed to).

## Hard requirements for every reviewer

1. Read the full paper (tex + the pages relevant to your focus as images).
2. Perform **at least 3 claim-vs-reality spot checks** against the ground-truth files above, each with
   `file:line` (or file+JSON-key) evidence. The paper must not claim anything the system does not do,
   and must not omit a load-bearing behavior that changes how a reader interprets results.
3. Judge the figures you are responsible for **as rendered images** (open the PNG pages), not from captions.
4. AEI taste: applied, engineering-informatics audience; values system clarity, module contracts,
   reproducibility, honest negative results; dislikes overclaiming, decorative figures, and
   text-only descriptions of what should be architectures/pipelines on paper.

## Output

Write your full report to `paper/review/round6/reports/<YOUR_ID>.md` and return (as your final message)
a summary ≤50 lines in this exact shape:

```
ID: <R?>
VERDICT: PUBLISH | MINOR_REVISION | MAJOR_REVISION
MUST-FIX (blocking, ≤8 items, each: page/section + one-line issue + evidence)
SHOULD-FIX (≤6 items, one line each)
SPOT-CHECKS (≥3 items: paper claim → ground-truth file:line → match/mismatch)
PUBLISH-READY: yes|no (one sentence why)
```

You are read-only: do NOT edit the paper, figures, scripts, or results.
