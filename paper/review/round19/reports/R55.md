# R55 — Figures & Visual Communication Review (Round 19, blind)

Reviewer remit: every figure and table judged **as rendered images** (110 dpi page PNGs in
`paper/review/round19/pages/`), plus caption–figure agreement, legibility, hatch/grayscale
robustness, figure–text number agreement, colour-convention compliance, CI consistency, and
float discipline. All colour samples below were read back pixel-wise from the rendered PNGs.

## Inventory (all floats located and inspected)

| Float | Page (render) | Verdict |
|---|---|---|
| Fig 1 `fig:arch` (TikZ, 5 planes) | p-13 | OK |
| Fig 2 `fig:pipeline` (TikZ, 2 rows + repair edge) | p-15 | OK, one caption nit |
| Fig 3 `fig:benchmark` (2 panels) | p-38 | OK |
| Fig 4 `fig:calib` (12-bar confidence chart) | p-40 | OK |
| Fig 5 `fig:casestudy` (2 signal panels + evidence strip) | p-41 | OK |
| Fig 6 `fig:perfault` (per-fault matrix) | p-46 | OK |
| Fig 7 `fig:forest` (Wilson forest plot) | p-47 | OK |
| Fig 8 `fig:matrix` (12×6 suite matrix) | p-56 | OK, one palette nit |
| Tables 1–10 | p-08, p-17, p-18, p-26, p-29, p-32, p-37, p-43, p-49, p-52 | OK |
| Tables A1, A2 | p-72, p-73 | OK |

No float is missing, duplicated, or out of order; all floats are top-aligned (the `\@fptop=0pt`
setting works — no vertically-centred float pages).

## 1. Caption–figure agreement (checked for all 8 figures)

- **Fig 1**: caption's "fourteen engines / 18 skill packages / CP-1…CP-9 / audit plane" all
  appear in the graphic; the engine row lists exactly the 14 names (claude…mock). Match.
- **Fig 2**: caption promises (i) amber checkpoint tags — present (CP-1…CP-9, amber text);
  (ii) the dashed amber repair edge — present, labelled "repair: best-of-3, global ≤5",
  re-entering Step 4; (iii) green boxes = hard gates Steps 7/8.5/9 — present exactly on those
  three boxes; (iv) bottom deliverables row reads right-to-left — arrows do run 6→7→8/8.5→9
  leftwards. Match. *Nit*: the amber text in Steps 2/3/4 boxes ("reuse store", "Phase 0–6",
  "3-state conclusion") is descriptive, not checkpoint tagging, which slightly dilutes the
  caption's "amber tags = checkpoint" encoding.
- **Fig 3**: panel (a) — SKAB bar is two hatched sky segments labelled 0/2; TEP is 5 dark blue
  + 1 amber labelled 5/6; IndPenSim 1 dark blue labelled 1/1. Exactly matches the caption's
  encoding and the printed fractions. Panel (b) — 12 rows, filled = rubric, open = judge, dotted
  gate at 90 with in-plot label "gate 90", dark blue faults vs grey controls. Match.
- **Fig 4**: colour/hatch legend as captioned: solid dark blue DET (6), hatched sky capped-CS
  ranked (SKAB valve 0.65, SKAB cavitation 0.58), solid amber capped-CS unranked (IDV3 0.60),
  hatched grey controls (0.78/0.88/0.88), dashed amber 0.70 ceiling line labelled
  "ceiling for capped verdicts only (0.70)". Value labels sit on white bboxes (confirmed at 3×
  zoom) and stay readable over the bars. Match.
- **Fig 5**: caption names BOTH floor-bin measures — the in-panel annotation (125 rows at
  30.0 L/min, 106 runs, mean run 1.18 samples) and the pipeline's separate 39.4 % floor-residence
  metric — and explicitly says they are "named and reported separately, not equated". Both are
  present exactly as described: the in-panel annotation in the top panel, the 39.4 % figure in
  the L3 evidence-chain strip. The pale-amber fault-window shading (rows 631–919) and pale-green
  floor-bin band are both visible. Match.
- **Fig 6**: cell colours verified by pixel sampling against the Okabe–Ito constants declared in
  the preamble: IDD Top-1 = (0,114,178) `iddblue`; capped CS = (230,159,0) `iddamber`; FE-correct
  = (0,158,115) `iddgreen`; FE-wrong = (213,94,0) vermillion; unscored/PCA-yes = pale grey
  (238,241,244). The caption's mapping is honoured cell for cell. Match.
- **Fig 7**: grey = FE baselines, blue = IDD rows, green = controls, exactly as captioned; the
  caption's caveat "interval labels for rows at 100% extend past the axis" is implemented (the
  3/3 label is set outside the axis and is fully visible); 50 % reference line labelled. Match.
- **Fig 8**: column encodings as captioned — col 1 verdict cells (hatched sky "CS 0.65 ranked" /
  "CS 0.58 ranked", amber "CS 0.60 unranked", green "normal", blue "Top-1"); col 2 "n/a" for the
  TEP control rendered as a grey cell (caption: execution-coverage gap, not a pass); col 3 "–"
  outside TEP; cols 4–5 numeric detection rates on a pale→dark ramp; col 6 "det."/"no"/grey
  "no (correct: control)". All present. Match.

**No caption-figure contradictions found.**

## 2. Legibility and grayscale robustness

- No element falls below the 6.5 pt floor at print size. The smallest type is Fig 5's
  six-line evidence-chain strip and Fig 8's in-cell text (≈7 pt equivalent); both are crisp in
  the vector PDFs and readable at 100 % zoom. TikZ figures use `\scriptsize`/`\tiny` under
  `\resizebox` to ~0.9×, still ≥ footnote size.
- Hatch rendering: diagonal hatching survives at page scale in Fig 3(a) (SKAB bar), Fig 4
  (sky CS bars and grey control bars) and Fig 8 col 1. Confirmed visible in a grayscale
  conversion of p-40 and p-46.
- Grayscale print behaviour: Fig 4 remains fully decodable (hatch + printed value labels).
  Fig 6's colour cells collapse to similar dark greys in grayscale, but every cell carries its
  own word ("wrong", "correct", "Top-1 (0.79)", "unscored", "yes", "no"), so no information is
  colour-only. Fig 8 likewise prints numbers in every ramp cell. Acceptable.

## 3. Figure–text number spot-checks (10 performed, ≥6 required)

1. Fig 4 all 12 confidences (0.65/0.58/0.78/0.79/0.60/0.88/0.88/0.88/0.75/0.82/0.80/0.89) ↔
   Table 8 verdict column and §7.3 text ("0.58–0.89", controls "0.78–0.88") — **match**.
   (A page-scale misread of the batch-1 control as "0.86" was resolved at 3× zoom: it prints
   "normal 0.88", consistent with the grading record.)
2. Fig 3(a) dataset bars ↔ §7.1 text (6/9 Top-1, top-k 8/9, IDV3 the single miss): blue units
   total 6, hatched units 2 (the two ranked CS verdicts), amber 1 — **match**.
3. Fig 3(b) SKAB-valve markers (filled rubric at 85, open judge at 90) and IDV11 rubric 90 ↔
   Table 8 (85 with −15 R4 deduction; 90 with −10 R6 deduction) and §7.1 deductions — **match**.
4. Fig 5 annotations (rows 631–919 = 289 samples; 210 switches; 125 rows/106 runs/1.18 mean run;
   39.4 % residence; 30.62 vs 31.73 L/min, −3.5 %; 0.990→0.947 A, −4.3 %; capped CS 0.65) ↔
   §7.3 text and Table 8 signature line — **match** (arithmetic self-consistent:
   919−631+1=289; 125/106=1.179≈1.18; 0.947/0.9898−1=−4.3 %).
5. Fig 7 six rows ↔ §8.1 text and Table 7: 7/11=63.6 % [35.4,84.8]; 9/11=81.8 % [52.3,94.9];
   5/6=83.3 % [43.6,97.0]; 6/9=66.7 % [35.4,87.9]; 8/9=88.9 % [56.5,98.0]; 3/3=100.0 %
   [43.8,100.0] — **match**, including the controls CI appearing identically in Table 7 AND
   Fig 7 as the convention requires.
6. CI convention: I recomputed Wilson intervals at z=1.96 for k/n = 3/3 → [43.85,100.0],
   6/6 → [61.0,100.0], 8/9 → [56.5,98.0], 5/6 → [43.6,97.0]; all printed intervals in Table 7
   and Fig 7 reproduce exactly. No z=1.645 leftovers anywhere — **convention holds everywhere**.
7. Fig 6 ↔ Table A1: per-fault cells and summaries (7/11, 9/11, 5/6 CI [43.6,97.0], shared
   detectable subset 4/4, 8/11 no-candidate regime) — **match**.
8. Fig 8 ↔ Table 9: IDV3 shown as 3/5 (rounded from T² 2.9 %/SPE 4.8 %), IDV4 48/100, controls
   1/1; bare-LLM column 9 "hit" + 2 "normal" + 1 "n/a" = 9/9 with 2/3 covered controls; FE-style
   column 6 green hits = 6/6 replication; FE-prot column 5 "det." + 1 orange "no" = 5/6 —
   **match**.
9. Fig 8 col 1 confidences ↔ Fig 4 (same 12 values) — **match** (internal cross-figure
   consistency).
10. Fig 2 repair-edge label ("best-of-3, global ≤5") ↔ §4.6/§5 text (≤3 per fault, global cap 5,
    anti-oscillation 0.50) and Fig 4's 0.70 ceiling line — **match**.

## 4. Claim-vs-reality spot checks (repository ground truth)

1. **Figure CI constant**: `paper/figures/make_figures.py:74` `def wilson(x, n, z=1.96)` with
   the comment "matches the released scorer" ↔ `scripts/benchmark/aggregate.mjs:28`
   `function wilson(k, n, z = 1.96)`. Figures, tables and text all print the same z=1.96
   intervals (verified by recomputation above) — **match**.
2. **Fig 3/4/5 data provenance**: `results/benchmark/metrics.json` (`by_dataset.skab.top1=0,
   topk=2; tep.top1=5; indpensim.top1=1; cdr_ci95=[35.42,87.94]; mean_rubric=97.9`;
   `per_case.skab_valve1_1 rubric=85, judge_score=90`) reproduces Fig 3(a)/(b) and Table 7
   exactly — **match**.
3. **Fig 5 annotations vs released run artifacts**:
   `workspace/diagnostic-runs/202609150453290_bench_skab_valve1_1/02_processed/anomaly_segment_analysis.json`
   keys `flow_in_window.switch_count_30_31=210`, `n_runs_at_30=106`, `mean_run_len=1.18`,
   `value_counts["30.0"]=125`, `anomaly_window.rows=289`, `current_in_vs_out.inside_mean=0.947 /
   outside_mean=0.9898`; `data_analysis_conclusion.json` contains window "631-919" and 39.4;
   `04_diagnostics/diagnosis.json` contains 30.62 and 31.73. Every annotation in Fig 5 and its
   caption is traceable to a released artifact — **match** ("every annotation is read from the
   released run artifacts" is literally true).
4. **Fig 4 confidences vs grading records**: `results/benchmark/gradings/*.json` `confidence`
   fields read 65/58/78/79/60/88/88/88/75/82/80/89 (e.g. `indpensim_batch001_control.confidence=88`,
   `skab_valve1_1.confidence=65`) — identical to the printed value labels — **match**.

## 5. Float discipline / page makeup

- All figures and tables are top-of-page floats; `\@fptop=0pt` top-aligns float pages; `placeins`
  keeps floats inside their sections. Drift between first citation and float is ≤2 pages
  everywhere I checked (e.g. Fig 6 cited §8.1 → p-46; Fig 8 cited §8.3 → p-56).
- A systematic ink-coverage scan of all 75 pages flags four short pages:
  - **p-18 (59 %)**: Table 3 alone on an otherwise empty page (float flush). Acceptable.
  - **p-55 (27 %)**: the worst page — five lines ending §8.5, then whitespace, with Fig 8 on the
    next page. Preprint-acceptable but the ugliest page in the build.
  - **p-72 (53 %)**: Table A1 alone on its float page, and p-73: Table A2 alone — the in-source
    comment ("% Float repack: [p] lets A1 and A2 share a single appendix float page") did not
    achieve its stated goal; the two appendix tables occupy two half-empty float pages.
  - **p-75 (49 %)**: final declaration page — normal.
- Verdict: acceptable as preprint typography (Elsevier re-typesets floats anyway), but p-55 and
  the A1/A2 split are worth a repack pass.

## 6. Defect list

Blocking: **none**. The figure programme is print-clean: every number I checked in every figure
reproduces from the released artifacts, the colour/hatch conventions are consistent across
Figs 3/4/8, captions agree with artwork, CIs are uniform at z=1.96, and nothing is illegible.

Should-fix (non-blocking):
1. p-55 stub (27 % filled): retune Fig 8's float declaration or add a strategic `\flushbottom`
   / text repack so §8.5's closing lines do not sit alone.
2. p-72/p-73: make Table A2 actually share A1's float page (or revert the stale in-source
   comment at main.tex:693 claiming it does).
3. Fig 8 PCA ramp dark end (46,110,99) is close in hue to the "hit" green (0,158,115); a more
   neutral ramp (grey→ink) would fully honour the caption's "does not reuse the semantic
   colours" claim. Printed cell numbers currently prevent any misreading.
4. Fig 2: recolour the non-checkpoint amber annotations ("reuse store", "Phase 0–6",
   "3-state conclusion") or restrict amber to CP tags, per the caption's own encoding.
5. Fig 6: the vermillion "wrong" and amber "capped CS" cells are both orange-family in colour
   (distinct in RGB and luminance, and disambiguated by column group and cell text); consider
   the darker vermillion for "wrong" to widen the gap.
6. Fig 5 evidence-chain strip and Fig 8 cell text sit at the ~7 pt floor; if the production
   format reduces the figures, re-check these two first.

## Bottom line

The figure programme is print-clean for AEI: eight figures and fourteen tables, all
top-aligned, all caption-consistent, all numbers traceable to released artifacts, one uniform
CI convention, colour-blind-safe Okabe–Ito palette with text/hatch redundancy that survives
grayscale printing. Remaining issues are page-makeup cosmetics typical of a preprint build.
