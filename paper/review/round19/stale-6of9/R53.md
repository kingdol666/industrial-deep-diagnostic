# R53 — Empirical Methods & Statistics Review (Round 19, blind)

Reviewer role: empirical methods and statistics. Focus: abstract; headline qualifiers (5/9, 4/6);
IDV3 miss; §4 two-layer caps; §5 C1 chronology + keyword sensitivity; execution-form disclosure;
Wilson CI convention (z=1.96); §7–§8 honesty including cost disclosure.
Method: read the full tex source and the rendered pages for all tables/figures under review, then
re-derived or re-counted every checkable quantity from the repository (own scripts, no trust).

---

## 1. Abstract

Verified against artifacts, all consistent:

- "6/9 faults ... (Top-1 66.7%, Wilson 95% CI [35.4%, 87.9%]; 5/9 under the pre-revision keyword
  set)" — `results/benchmark/metrics.json`: top1=6, fault_cases=9, cdr_ci95=[35.42, 87.94]; the
  5/9 qualifier is printed in the abstract itself (p.1, verified as rendered).
- "ranks the true mechanism among the candidates in two more" — topk=8 in metrics.json (6 + 2).
- "the ninth (TEP IDV3) is reported as a mechanism-level miss" — abstract states it plainly;
  metrics.json per_case tep_d03_hard: top1=false, topk=false (survivors exclude it, consistent
  with the "miss" framing); rendered Fig 3 shows the amber IDV3 segment; Fig 4 shows the amber
  CS 0.60 bar; Table A1 lists "CS 0.60 (site indistinguishable)". The miss is stated in the
  abstract, §6.2 ("We report this as what it is---a mechanism-level miss on the hardest fault"),
  §7.1, and the appendix. MATCH.
- "three scenarios ... return capped competing_set verdicts naming the missing measurement" —
  valve 0.65 / cavitation 0.58 / IDV3 0.60 in Table 8 and Fig 4. MATCH.
- "no control raises a false alarm" — control_pass=3, false_alarms=0 (metrics.json). MATCH.
- "On the TEP subset IDD is Top-1 correct on 5/6 faults (4/6 under the pre-revision keyword set)"
  — both qualifiers present in the abstract. MATCH.
- "a bare call to the same model family ... resolves 9/9 faults---more than the pipeline" —
  `results/benchmark/baselines.json` summary: "single_llm_same_digest_ablation": "TEP 6/6,
  non-TEP 3/3 (all 9/9 faults hit)". MATCH, and the abstract does not hide the unfavorable
  direction.

## 2. Headline qualifiers (5/9, 4/6) — coverage sweep

All occurrences in main.tex (excluding the false positive "Steps 7/8.5/9" at line 262):
lines 56 (abstract, both), 82 (contribution 4, both), 355 (§5 C1, both), 445 (§6.1, 5/9), 448
(Table 7 caption, both), 543 (§7.1, both), 556 (Fig 6 caption, 4/6), 563 (Fig 7 caption, both),
671 (conclusion, both), 695 (Table A1 caption, 4/6 + mechanism of the IDV1 flip).
Every headline statement of 6/9 or 5/6 carries the pre-revision qualifier, including all three
figure/table captions a skimming reader would cite. One exception: §9 (p.57, tex line 643)
restates "Top-1 = 66.7%" in the statistical-power paragraph without the 5/9 note. Minor, but for
consistency the qualifier should appear there too (the sentence is precisely about the headline
estimate). SHOULD-FIX.

## 3. §4 two-layer caps — verified against enforcing code and released artifacts

- Agent-side: `.claude/skills/industrial-diagnostician/SKILL.md` "Core Rules": "Confidence
  ceilings: INDISTINGUISHABLE ≤ 65, COMPETING_SET ≤ 70, [PARAM_AMBIGUITY] ≤ 50"; no DETERMINED
  cap documented. MATCH with §4 ("0.70 for CS, tightened to 0.65 for its INDISTINGUISHABLE
  subclass, and 0.50 whenever the anti-oscillation rule (code name PARAM_AMBIGUITY) is active ...
  the agent-side protocol documents no DET cap").
- Machine gate: `scripts/benchmark/judge-rubric.mjs` (R4 block, ~line 133):
  `const ceiling = D.diagnosis_type === 'COMPETING_SET' ? 0.70 : 0.90;` — exactly the paper's
  "confidence ≤ 0.90 for DET and ≤ 0.70 for CS ... the gate's flat non-CS 0.90 ceiling also
  covers ND". MATCH.
- Reason-coded ceilings: `.claude/skills/industrial-diagnostician/schemas/confidence_schema.json:121`
  enum = [INDISTINGUISHABLE_COMPETING_SET, UNSORTED_DATA_LAG_CLAIMS, PRODUCT_SPECIFIC_ONLY,
  ALL_CRITERIA_VERIFIED, DIRECT_MEASUREMENT] — the paper's list of five reasons verbatim. MATCH.
- Worked example: canonical IDV4 run
  `workspace/diagnostic-runs/202609141854259_bench_tep_d04_reactor_cooling_step/04_diagnostics/confidence.json`:
  overall score 75; confidence_ceilings_applied = [{hypothesis_id: "H1", ceiling: 75, reason:
  "DIRECT_MEASUREMENT", ...}]. MATCH with §4 and §7.5 (case study, det 0.75).
- Five-factor weights: `diagnostic-quality-check.mjs` (~line 151) factorMax = {statistical 25,
  physical 25, temporal 20, confounds 20, symptom 10}. MATCH with §4 (25/25/20/20/10).
- Elimination contract: `schemas/diagnosis_schema.json` exclusion_confidence {"minimum": 90,
  "maximum": 100}. MATCH with "schema floor of exclusion confidence ≥ 0.90".

## 4. §5 C1 chronology table and keyword sensitivity — recomputed

Journal (`results/benchmark/journal.jsonl`) and git, checked row by row against Table 4 (p.26):

- Pre-switch batch 09-14 20:06:32–33Z: exactly 3 faults (skab_valve1_1, skab_cavitation_13,
  tep_d01) + 2 controls; tep_d01 graded top1=false, kw_hits=[], overconfident=true
  (DETERMINED with no keyword hit) — the "superseded pre-switch batch disclosed rather than hidden".
  MATCH.
- Switch completing 20:09:48–49Z: tep_d01 flips to top1=true, kw_hits=[进料配比, 配比, a:c,
  stream 4]; the other two faults unchanged. "2 outcomes unchanged, TEP IDV1 flips at 20:09:48Z".
  MATCH.
- First later batch 21:17:16Z (tep_d03/d04/d07/d11), every subsequent batch calibrated (tep_d01
  hits 配比 in all; tep_d03 topk=false throughout). MATCH with "every later canonical grading
  uses the calibrated set (first later batch 21:17:16Z)".
- Canonical executions 09-14 10:43–20:54Z: run-dir names are UTC+8 local; twelve canonical runs
  span 20260914-184351 → 20260915-045447. MATCH.
- Commit 221d061: `git log` = 2026-09-15T19:20:18+08:00 = 11:20:18Z. MATCH with "09-15 11:20Z".

Independent sensitivity recompute (my own code, replicating the grader's corpus construction and
matching at `scripts/benchmark/zcode_direct_pipeline.mjs:373-390` — flat() deep-collect, text =
primary_finding + surviving {name, physical_logic_chain, supporting_evidence,
ontology_data_physics_proof}, case-insensitive substring):

- Case tep_d01_ac_feed_ratio, canonical run 202609150454126, diagnosis_type=DETERMINED.
- Pre-revision keywords (git 221d061~1): ["a/c feed","feed ratio","进料比","stream 4","a and c",
  "成分"] → 0 hits → top1=false.
- Calibrated keywords: adds 进料配比/配比/a:c (9 total) → sole hit ["配比"] → top1=true.
- This is exactly the paper's claim: "TEP IDV1 becomes a keyword hit only under the calibrated
  set (its sole hit is the added synonym), so Top-1 is 5/9 pre-revision versus 6/9 calibrated
  (TEP subset: 4/6 → 5/6), while top-k stays 8/9". `results/benchmark/keyword_sensitivity.json`
  agrees (changed scenarios tep_d01 up, tep_d03's spurious top-k removed; 5/9 vs 6/9; top-k 8/9
  both). MATCH — reproduced independently.

Leakage spot-check: the released blind brief
`results/benchmark/briefs/tep_d01_ac_feed_ratio.brief.json` contains none of the grading
keywords (配比/进料配比/a:c absent); the only fault-word occurrence is the case_id itself, which
the paper discloses as the residual identifier channel (§9). Consistent.

## 5. Execution-form disclosure vs canonical event logs — recount MISMATCH

Paper (§5, "Execution form", tex line 365): "eight of the twelve canonical run logs carry an
explicit marker (a dedicated field on some runs, disclosure or remediation notes on others)".

My recount over the twelve canonical `.pipeline_events.jsonl` files:

- Dedicated-field markers (9 runs): skab_valve1_1 (`mode:"full"`, `inline_execution:true` ×7,
  reason "Agent tool unavailable in this harness; stage executed inline under its own skill
  protocol" — quoted correctly in the paper); tep_d00 (`harness:"inline-degraded"` + note "Agent
  dispatch tool unavailable in this session..." + backfill remediation note); tep_d03
  (`inline_non_agent_harness` ×5); batch001 (`harness_mode:"inline"` ×16); batch093
  (`inline_protocol` ×8); tep_d04 (`harness_mode:"inline"` + Chinese `harness_disclosure`);
  tep_d07 (`harness_mode:"inline_substituted"` ×2 + full disclosure note); tep_d11
  (`inline_no_agent_tool` ×10, `inline_deterministic_fastpath`); tep_d14
  (`inline_strict_protocol` ×18 + note).
- Note-only markers (2 runs): tep_d01 (`disclosure:"inline execution (Agent tool unavailable in
  this harness run)"` + `remediation` backfill note + run_completed disclosure);
  skab_normal_control (finalize note "no Agent dispatch tool in this environment; each stage
  executed inline strictly under its own skill protocol...").
- No marker (1 run): skab_cavitation_13 (only incidental "dispatch" fields of step_start events
  and the ontology fast-reuse note).

Total: **11 of 12**, not 8 of 12. The claim is stale or miscounted. Direction is conservative
(more logs carry the marker than claimed; the load-bearing statement "the driver itself exposes
no spawn tool" is consistent with `zcode_direct_pipeline.mjs`, and the SKAB-valve quotation is
verbatim-accurate), but a precision error in the paragraph whose purpose is precise disclosure
must be corrected. MUST-FIX (change "eight" to "eleven", or re-derive the intended stricter
criterion and state it explicitly).

## 6. Wilson CIs at z=1.96 — all recomputed by hand

Convention verified at the source: `scripts/benchmark/aggregate.mjs:28` `function wilson(k, n,
z = 1.96)`; `paper/figures/make_figures.py:74` `def wilson(x, n, z=1.96)` with the comment
"matches the released scorer". My hand computations (z=1.96) vs the paper:

- 6/9 → [35.4, 87.9] ✓ (Table 7, Fig 7, abstract, §9)
- 8/9 → [56.5, 98.0] ✓ (Table 7, Fig 7)
- 6/6 → [61.0, 100.0] ✓ (Table 7)
- 3/3 controls → [43.8, 100.0] ✓ — present in **both** required locations: Table 7 (p.37, as
  rendered) and Fig 7 (p.47, green row "3/3 = 100.0% [43.8, 100.0]", as rendered)
- 5/6 → [43.6, 97.0] ✓ (§7.1, Fig 7, Table A1)
- 7/11 → [35.4, 84.8] ✓ and 9/11 → [52.3, 94.9] ✓ (§7.1, Fig 7)

No Wilson interval anywhere deviates from the z=1.96 convention (the commonly seen 3/3
"conservative" interval [29.2, 100.0] from z=1.0 or Clopper-Pearson does not appear). MATCH on
all seven distinct printed intervals. Caption of Table 7 also flags the non-independence of the
determined-precision row — good practice.

## 7. Cost disclosure — median recomputed across all twelve event-log spans

Claims: §6.4 "56–220 minutes ... (median ≈109 minutes across the twelve canonical runs; event-log
spans)"; §7.1 and §9 repeat the same numbers.

Recomputation from the twelve canonical logs (first→last event timestamp):

55.6 (d14), 65.9 (d00), 69.4 (valve), 74.7 (cavitation), 81.1 (skab_ctrl), 86.6 (d01), 131.5
(d07), 136.5 (d11), 141.1 (d04), 146.4 (d03), 217.4 (batch001), 220.1 (batch093) minutes.

min 55.6 ≈ 56 ✓; max 220.1 ≈ 220 ✓; n=12 (even), median = mean of 6th/7th order statistics =
(86.6 + 131.5)/2 = **109.05 ≈ 109** ✓. The paper's ≈109 is the correct even-n median, not a
rounded pick of a middle element. MATCH. The §9 cost paragraph also concedes the pipeline is
"orders of magnitude more expensive" than classifiers and far slower than prompt baselines —
honest.

## 8. §7–§8 honesty — further checks

- Baselines table vs `results/benchmark/baselines.json`: FE-protocol replication 6/6 under
  fe-style, strict-with-candidates, and strict-no-candidates; FE official code 5/6; single-LLM
  9/9; PCA IDV3 T² 2.9%/SPE 4.8%, IDV4 SPE 100% vs T² 48.8%, controls 1% — all MATCH (Table 9,
  p.49, as rendered).
- Bare-LLM control coverage: only skab_normal_control and indpensim_batch001_control were run in
  the bare arm (tep_d00 absent); the paper states this three times (Table 9 row scope "2
  controls", §8 text, Fig 8 caption "n/a ... execution-coverage gap, not a pass"). Honest.
- Ablation framing: "the bare same-model call is not merely equal to the full pipeline but ahead
  of it: 9/9 strict hits versus the pipeline's 6/9" — the unfavorable result is the headline of
  §8.3, the abstract, and the conclusion. Exemplary.
- Repair governance: my count over the twelve logs — 11 runs with zero repair_rounds;
  batch001_control repair_rounds=1 (judge event and run summary both say 1). MATCH with "eleven
  scenarios required zero repairs; one control required a single repair round".
- Determinism: `suite_determinism.json` compared=45, identical=45, verdict DETERMINISTIC, with
  the paper's own scope note that it covers the deterministic suite arms only. MATCH.
- Stability: `stability_report.json` summary era_case_pairs=15/15 (12 v1-era + 3 v2-era pairs,
  counting n_runs_proven≥2 per case-version); seeded retest `retest_selection.json`: 4 recorded
  rounds, round 4 = seed 149290398, u=0.803782468 → tep_d11, with rounds 2–4 all selecting
  tep_d11 ("three of which selected TEP IDV11"); tep_d11's current era holds a single canonical
  run → the paper's INSUFFICIENT-runs disclosure is accurate. All MATCH (§7.4).
- Rubric deductions: `rubric.json` — skab_valve1_1 score 85 (R4 fail: type COMPETING_SET not in
  allowed ["DETERMINED"]; confidence 0.65 within the 0.70 ceiling), tep_d11 score 90 with
  R6_physics pass=false, all others 100 → mean 97.9, "ten at 100, one at 90, one at 85". MATCH
  exactly with Table 7/8.
- Judge gate: metrics.json judge scores {90,93,96,94,92,97,91,93,92,90,93,96} → mean 93.08 ≈
  93.1, range 90–97, all ≥90. MATCH.
- RAG channel (§3.2, §9): canonical tep_d01 event log records `fallback_directive` and
  `unavailable_directive_fallback` events (the paper attributes explicit fallback events to
  tep_d01 — correct for the canonical 09-15 run, not the superseded 09-14 one);
  skab_valve1_1 `rag_deep_understanding.json` has retrieval_meta (the 21-chunk successful
  retrieval); tep_d14 rag_available=false with an L5-fallback reason string; cavitation and
  skab_normal_control have no RAG artifact (fast-reuse). All MATCH.
- Package inventory (§3.1): `ls .claude/skills/*/references/agent-protocol.md | wc -l` = 12 of
  18 packages; the "3+3.3" VLM label exists in the orchestrator SKILL.md dispatch table (lines
  57, 82); 40 run directories referenced by §7.4 exist. MATCH.
- Case-study figure ground truth (Fig 5): window rows 631–919 shading, "210 switches 30↔31",
  pump 0.990→0.947 A (−4.3%), 39.4% floor dwell — as claimed; the caption's honest separation of
  the 125-row/106-run annotation vs the 39.4% floor-residence metric is the right way to handle
  two different measures.

## 9. Figures as rendered (print quality)

- Fig 3 (p.38): legible; amber IDV3 segment distinct; hatched CS handling per caption. OK.
- Fig 4 (p.40): confidence bars with 0.70 dashed ceiling labelled "for capped verdicts only";
  amber IDV3 bar; overconfidence note on the axis. Clean. OK.
- Fig 5 (p.41): dense but readable; annotations match released artifacts. OK.
- Fig 6 (p.46): matrix legible; caption color word "vermillion = FE-wrong" vs the rendered
  orange/amber IDV1-wrong cell — the wrong-cell hue is close to the "capped CS, not ranked"
  amber used in Fig 8; align the color naming. Minor.
- Fig 7 (p.47): forest plot with all six interval labels incl. controls [43.8, 100.0]; rows at
  100% handled as captioned. OK.
- Fig 8 (p.56): per-scenario matrix legible; PCA ramp visually independent of semantic colors as
  claimed; bare-LLM TEP-control n/a marked. OK.

## 10. Verdict rationale

Every quantitative claim I could recompute (≈15 distinct checks, 4+ fully independent
recomputations) reproduced exactly, including two performed from raw logs with my own code (the
even-n median 109.05; the tep_d01 keyword flip via the single added synonym 配比 under the
grader's exact matching). Uncertainty is quantified uniformly (z=1.96 Wilson everywhere), the
design's weaknesses (n=9, single recorded execution, author-designed rubric/keywords, one model
family, bare-LLM superiority on accuracy) are disclosed prominently and repeatedly, and the
repair/selection effects are counted rather than asserted. The defects found are: one factual
miscount in the execution-form disclosure ("eight of twelve" vs the actual eleven of twelve),
one lowercase-sentence typo in §6.4 (visible on p.34: "...event-log spans).  the entire chain
(prepare, ...)"), and one missing qualifier restatement in §9. None of these threatens the
headline claims; the first must nonetheless be fixed because that paragraph's sole purpose is
precise disclosure and the number is reader-checkable.

Decision: MINOR_REVISION.
