# FORMAT AUDIT — Elsevier / Advanced Engineering Informatics compliance

Manuscript: `paper/main.tex` (elsarticle, `preprint,10pt`; compiled `main.pdf`, 45 pages, letter).
Audited: 2026-09-18. Evidence: source greps of `main.tex`/`refs.bib`/`highlights.md`, `pdfinfo`/`pdfimages`/`pdftotext` on `main.pdf`, `main.log`, `main.bbl`, visual check of figure PNGs.

## Compliance table

| # | Item | Verdict | Evidence |
|:--|:--|:--|:--|
| 1 | Title | **PASS** | "Trustworthy agentic diagnosis for industrial processes: evidence-graded competing-hypothesis reasoning evaluated against per-fault literature baselines" = **14 words** (≤ ~20). No abbreviations (no LLM/TEP/IDD/RCA). `\title{}` at main.tex:48. |
| 2 | Author line | **PASS** (minor note) | `\author[ustc]{ZhangHaoZheng}` + `\address[ustc]{Institute of Advanced Technology, University of Science and Technology of China, Hefei 230088, China}` + `\ead{zhanghaozheng@ustc.edu.cn}` (main.tex:50-52). *Note:* the name is one token without a separable given name/surname; Elsevier indexing prefers distinct "Given Surname". |
| 3 | Abstract | **PASS** (minor notes) | Single paragraph (main.tex:55-57); **242 words** (≤ ~250); zero `\cite` in abstract. LLM and IDD both defined at first use. *Notes:* "Wilson 95% CI" — "CI" not expanded in the abstract; "evidence grading (L1–L7)" labels unexpanded (labels, not acronyms — borderline). |
| 4 | Keywords | **PASS** | `\begin{keyword}` after abstract (main.tex:59-61): **6 keywords** (Fault root-cause analysis; Large language models; Multi-agent systems; Competing-hypothesis reasoning; Evidence grading; Reproducible benchmark) ≤ 7. |
| 5 | Highlights | **PASS** | `highlights.md`: **5 bullets** (3–5 required). Char counts **including spaces**: 80, 85, 85, 81, 74 — all ≤ 85. *Note:* bullets use abbreviations "RCA" and "L1-L7" (acceptable in highlights, but spell-out is safer). |
| 6 | Declarations block | **PASS** | All five present as `\section*`: CRediT authorship contribution statement (:675), Declaration of competing interest (:678), Funding (:681), Data availability (:684), Declaration of generative AI (:687). *Note:* block is placed after the Appendix rather than before the references (see item 10). |
| 7 | References | **PASS** (minor note) | `\bibliographystyle{elsarticle-num}` (:600); `main.bbl` = numeric `\bibitem` list, **46 entries**. Cross-check: **46 cited keys, 46 bib entries, zero orphans in either direction** (comm diff of extracted keys). Identifier coverage: 45/46 entries carry a DOI; the only exception is `heuer1999psychology` (a printed CIA book — no DOI exists; acceptable, books are exempt in practice). Spot-check of 6 entries — all metadata correct: `downs1993plant` (CCE 17(3):245–255, 1993, 10.1016/0098-1354(93)80018-I), `hill1965environment` (Proc R Soc Med 58:295–300, 1965, 10.1177/003591576505800503), `chow1970optimum` (IEEE Trans Inf Theory 16(1):41–46, 1970, 10.1109/TIT.1970.1054406), `parasuraman2000model` (IEEE Trans SMC-A 30(3):286–297, 2000, 10.1109/3468.844354), `rieth2017additional` (Harvard Dataverse, 10.7910/DVN/6C3JR1), `tuli2022tranad` (PVLDB 15(6):1201–1214, 2022, 10.14778/3514061.3514067). Authors "and"-joined full names, journals/volumes/pages/years present throughout. |
| 8 | Figures | **PARTIAL** | All 9 `\includegraphics` targets exist in `figures/`. Effective resolution (`pdfimages -list`): **886–1046 ppi per image** — all ≥ 300 DPI with large margin. Captions below figures (`\includegraphics` then `\caption` in every figure env). No in-figure title duplicating captions: matplotlib figures carry only short panel labels "(a)…/(b)…" (make_figures.py:129,150) and the HTML-rendered architecture/pipeline diagrams use plane/stage labels that are diagram content, not titles (fig_architecture.png inspected visually). Citation order: Fig. 1 (arch, :129) → 2 (pipeline, :150) → 3 (benchmark, :367) → 4 (calib, :456) → 5 (casestudy, :456) → 6 (perfault, :464) → 7 (forest, :464) → 8 (matrix, :532) — ascending. **FAIL sub-item: Fig. 9 (`fig:consistency`, fig_consistency.png, declared :553) is never cited in the text** (`grep "ref{fig:consistency}"` = 0 hits). |
| 9 | Tables | **PARTIAL** | All 10 tables booktabs-only (`\toprule/\midrule/\bottomrule`; no `|` in any column spec). Captions above tabulars in every table env. Citation order (body text): Tables 1 (related, :97) → 2 (modules, :200) → 3 (checkpoints, :200) → 4 (artifacts, :283) → 5 (bench, :326) → 6 (overall, :367) → 7 (mech, :404) → 8 (ablation, :492) → A1 (:464/611) → A2 (:612) — in order. **FAIL sub-item: Table 9 (`tab:stability`, declared :537) is never cited in the text** (0 `\ref` hits). Minor: Table 2's caption cites Table 3 (acceptable cross-reference); `\resizebox` used on several tables (permitted). |
| 10 | Structure | **PASS** (minor note) | Introduction (1) → Related Work (2) → System Architecture (3) → Diagnostic Methodology (4) → Evaluation Framework (5) → Experimental Setup (6) → Results (7) → Comparison/Ablation/Stability/Case Study (8) → Discussion (9) → Conclusion (10) → References → Appendix A → Declarations. All sections numbered; no orphan subsections (every subsection has parent lead-in text). Appendix: single `\appendix` section "Appendix A" with `\setcounter{table}{0}` + `\thetable = A\arabic` → **A1/A2 numbering consistent**. *Note:* the five Declarations `\section*` blocks sit **after** the Appendix (post-bibliography); Elsevier's typical order is Conclusion → Declarations → References → Appendix. Content-wise complete; order is cosmetic but worth normalising. |
| 11 | Math/notation | **PASS** | Exactly two display equations, both numbered: Eq. (1) `eq:ceiling` (:222) and Eq. (2) `eq:verdict` (:227); both referenced via `\eqref` (:235, :245-246). **No** `\[…\]`, `equation*`, `align*`, or `gather*` anywhere — no unnumbered display equation is text-referenced. |
| 12 | Build artifacts | **PASS** | `main.tex`: no TODO/XXX/FIXME/TBD/LOREM; no doubled words (`\b(\w+) \1\b` scan clean); no literal `??`. Compiled PDF (`pdftotext`): no `??` (all refs resolved). `main.log`: **zero warnings** (no undefined references/citations, no multiply-defined labels). |
| 13 | Language consistency | **PASS** (minor notes) | Dominant = **British** (-ise family ×40+: finalisation ×15, quantised/quantisation ×4, localised/localisation ×3, summarises, specialised, computerised, standardised, visualises, synthesised, synchronised, formalised; "judgement", "grey", "Conceptualisation" in CRediT). Mixed cases found: **one American "realized"** (main.tex:210, "contract realized as a capability table"). "artifact" (×70, American form vs BrE "artefact" ×0) — used 100% consistently, so a uniform convention rather than a mix; acceptable in CS/engineering usage. Fix the single "realized" → "realised" for full consistency. |
| 14 | hyperref colour links | **PASS** (not flagged) | `colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue` (:14) — fine for review; per instruction nothing flagged. |

## Must-fix items (2)

1. **Cite Fig. 9 in the text.** `fig:consistency` (the consistency-audit figure, main.tex:553-558) is never referenced. Add e.g. `Fig.~\ref{fig:consistency}` in Section 6.4 (Verdict Stability) where the retest pair is discussed (~line 551). Elsevier requires every figure to be cited in the text, in numerical order — placing the citation in §6.4 before Fig. 8's citation at :532 would break order, so either cite it after the Fig. 8 reference point or move the figure/citation accordingly (easiest: cite it at the end of §6.4, i.e., source position after the Fig. 8 citation at :532 is already satisfied — Fig. 9 numbered after Fig. 8, citation anywhere after :532 preserves ascending order).
2. **Cite Table 9 in the text.** `tab:stability` (Verdict Stability table, main.tex:537-550) is never referenced. Add `Table~\ref{tab:stability}` in §6.4, e.g., "The two executions agree (Table~\ref{tab:stability})…" — Table 8 is last cited at :492-532 region, so a citation in §6.4 keeps tables in ascending citation order (Tables 1–8 already cited before it).

## Minor (non-blocking) notes

- Author name "ZhangHaoZheng" — provide separable given name + surname for Elsevier metadata.
- Abstract: expand "CI" (confidence interval) at first use; consider expanding "L1–L7" or glossing as "evidence levels".
- Declarations block placed after the Appendix; conventional Elsevier order is before the references.
- "realized" (:210) → "realised" (only American -ize/-ise slip; "artifact" is consistent and acceptable).
- Highlights use abbreviations RCA / L1-L7; consider spelling out.

## Format verdict

**FORMAT VERDICT: NON-COMPLIANT** (2 must-fix items — both one-line citation additions; everything else passes, several with minor notes)

Must-fix count: **2** (uncited Fig. 9 `fig:consistency`; uncited Table 9 `tab:stability`)
