# R75 — FIGURES & VISUAL COMMUNICATION review (Round 24, blind)

Reviewer scope: every figure and table judged **as a rendered image** (110 dpi page PNGs in
`paper/review/round24/pages/`), plus the vector sources (`paper/figures/*.pdf`, generated by
`paper/figures/make_figures.py`) and the LaTeX log for float/overfull behaviour. Blindness rule
respected: nothing under `paper/review/` was read except this briefing and the `pages/` PNGs.

Rendered build: 75 pages. Figure/table locations verified from the compiled PDF:
T1 p8 · F1 p13 · F2 p15 · T2 p17 · T3 p18 · T4 p26 · T5 p29 · T6 p32 · T7 p37 · F3 p38 ·
F4 p40 · F5 p41 · T8 p43 · F6 p46 · F7 p47 · T9 p49 · T10 p52 · F8 p56 · A1 p72 · A2 p73 ·
Algorithm 1 p21.

---

## 1. Caption–figure agreement (all 8 figures)

| Fig (page) | Verdict |
|---|---|
| F1 Skill execution architecture (p13) | MATCH. Five planes drawn top-to-bottom; engine row lists exactly 14 engines (claude · omp · codex · dsh · opencode · gemini · copilot · cursor · crush · goose · qwen · pi · hermes · mock); "18 skill packages / 14 agents" and the green audit plane match the caption. |
| F2 Pipeline flow (p15) | MATCH. Caption: amber checkpoint tags (rendered amber, legible); dashed amber repair edge labelled "repair: best-of-3, global ≤5"; bottom row reads right-to-left with leftward arrows as stated; green boxes on exactly Steps 7, 8/8.5, 9 as the caption's "hard gates (Steps 7/8.5/9)" sentence claims. Post-relabel text prints ≥8 pt-equivalent; all box labels and CP tags readable in the page render. |
| F3 Benchmark outcomes (p38) | MATCH. (a) SKAB bar fully hatched sky "0/2", TEP blue-to-5 + amber 5–6 "5/6", IndPenSim blue "1/1"; controls excluded as captioned. (b) filled = rubric, open = judge, blue faults / grey controls, dotted gate at 90 with in-plot "gate 90" tag. Marker semantics are caption-defined (code comments confirm the legend omission is deliberate). |
| F4 Confidence per scenario (p40) | MATCH. Solid dark-blue DET bars; hatched sky CS bars (valve 0.65, cavitation 0.58); single solid amber CS bar (IDV3 0.60) with dark text; three hatched grey "normal" bars; amber dashed 0.70 ceiling with right-aligned in-plot label "ceiling for capped verdicts only (0.70)". Every value label carries a white semi-opaque bbox — verified in the render: the ceiling dash does not strike through any label. |
| F5 Case study (p41) | MATCH. Caption names **both** floor-bin measures and the figure shows exactly those two, separately: in-panel annotation "lowest quantisation bin (30 L/min): 125 rows, 106 runs, mean run 1.18 samples" vs evidence-strip "39.4% residence at lowest quantised level vs 0.0% outside". Pale-amber fault window, pale-green floor band, white bboxes on all three annotations; evidence chain strip sits fully inside the canvas at 8 pt. |
| F6 TEP per-fault matrix (p46) | MATCH. Cell colours exactly per caption: dark blue IDD Top-1 (white text), amber capped CS with dark text (IDV3 0.60), green FE-correct, vermillion "wrong" (IDV1/GPT-4o only), pale grey unscored and PCA-"yes", darker grey PCA-"no" (IDV3/IDV4); blue divider isolates the IDD column. |
| F7 Forest (p47) | MATCH. Grey FE rows, blue IDD rows, green controls; caption states z = 1.96; the 100% controls row label "[43.8, 100.0]" indeed extends past the axis as captioned; "50%" reference line labelled. |
| F8 Suite matrix (p56) | MATCH. Six columns as captioned; column 1 uses the semantic palette (hatched sky ranked CS ×2, amber unranked CS ×1 with dark text, blue Top-1 ×6, green "normal" ×3); column 2 "n/a" grey for TEP control (caption explains: coverage gap, not a pass); column 3 "–" outside TEP; columns 4–5 sequential teal ramp (darker = higher) with no reuse of semantic hues; column 6 grey "no (correct: control)" for the TEP control; dataset group separators present; 6-patch legend below matches the cells. |

No caption contradicts its artwork anywhere. Small-caps/typewriter verdict naming is consistent
across captions, panels, and body text.

## 2. Colour-convention audit (task convention vs rendered reality)

- IDD dark blue = DETERMINED: consistent in F3(a,b), F4, F6, F8 col 1 (the old second green is
  gone — batch-93 is blue in both F4 and F8; `make_figures.py` comments record the unification). PASS.
- Hatched sky = capped CS, true mechanism ranked: F3(a) SKAB bar, F4 valve+cavitation, F8 col 1
  valve+cavitation — all carry the black '///' hatch. PASS.
- Amber (near-black text) = capped CS unranked: F3(a) TEP 5–6 segment, F4 IDV3, F6 IDV3 cell,
  F8 IDV3 cell — all solid amber with dark text. PASS.
- Green = FE hits / control cells: F6 FE-correct, F8 "hit"/"normal"/"det." and col-1 controls.
  VERM reserved for wrong/miss. PASS.
- Grey = FE-unscored / not-in-scope: F6 pale-grey vs grey-"no" two-step grey is distinguishable
  in the render. PASS.
- Teal ramp (F8 cols 4–5) is orthogonal to the semantic palette as claimed. PASS.

Grayscale/hatch check: sky (#56B4E9) and amber (#E69F00) are near-identical in luminance, and the
hatch is what actually carries ranked-vs-unranked — it renders clearly at 110 dpi and at 220 dpi
in the source PNGs, including inside the small F8 cells and legend patches. Good print design.
One residual (cosmetic): in F4 the hatched **grey** control bars and hatched **sky** CS bars share
the same '///' hatch with only ~10% luminance difference; the coloured value labels ("normal 0.88"
grey vs "CS 0.65" navy) and row names disambiguate, so this is acceptable, but a different hatch
angle for controls would make it robust.

## 3. Legibility audit (threshold: nothing below 6.5 pt printed)

`make_figures.py` sets all figure canvases to the final print width (PRINT_W = 5.4 in), so nominal
matplotlib point sizes are the printed sizes. Smallest text in the programme: 7.0 pt (F8 y-tick
scenario labels), 7.2 pt (F4/F7 value labels), 7.3 pt (F4 y-labels), 7.6–8.3 pt cell text.
All ≥ the 6.5 pt floor; nothing was found un-readable in any page render, including F8's hatched
cells and F5's three-line evidence strip. TikZ figures F1/F2 (post-relabel) print ≥8 pt-equivalent.
Figures use Arial with `pdf.fonttype=42` (TrueType embedded, editable) — satisfies Elsevier's
digital-artwork requirements. Vector PDF everywhere; no rasterised text. PASS.

## 4. Figure–text number spot-checks (7 performed; all MATCH)

1. **F3(a) fractions** 0/2 + 5/6 + 1/1 sum to the text/Table 7 headline 6/9, and the amber
   5–6 segment is exactly the one mechanism-level miss (IDV3) — matches Section 7.1 and Table 7.
2. **F3(b)** valve rubric marker at 85 (below the 90 gate) = Table 8 rubric 85 and the stated
   −15 R4 deduction; IDV11 at 90 = the −10 R6 deduction; ten markers at 100 = Table 7's
   "ten at 100"; all open judge markers ≥ 90 = Table 7 range 90–97.
3. **F4 bar values** 0.79/0.60/0.75/0.82/0.80/0.89/0.88 (faults), 0.65/0.58/0.60 (CS),
   0.78/0.88/0.88 (controls) = Table 8's verdict/confidence column; the 0.58–0.89 span matches the
   Section 7.3 sentence; ceiling line at 0.70 = the CS cap of Section 4.1.
4. **F5 annotations** (rows 631–919; 210 switches; 125 rows/106 runs/1.18 mean; 0.947 vs 0.990 A,
   −4.3%; 30.62 vs 31.73 L/min, −3.5%; 39.4%; verdict 0.65) = Table 8 SKAB-valve signature,
   Section 7.3 narrative, and the released run artifacts (see §5.2).
5. **F6 IDD column** 0.79/0.60/0.75/0.82/0.80/0.89 and FE cells (wrong/correct/unscored) =
   Table A1 row-for-row; PCA "no" exactly on IDV3/IDV4.
6. **F7 labels** 7/11 = 63.6% [35.4, 84.8]; 9/11 = 81.8% [52.3, 94.9]; 5/6 = 83.3% [43.6, 97.0];
   6/9 = 66.7% [35.4, 87.9]; 8/9 = 88.9% [56.5, 98.0]; 3/3 = 100.0% [43.8, 100.0] — each matches
   the body text (Section 8.1) and Table 7 rows. I recomputed all six Wilson intervals at z = 1.96
   by hand: every printed bound is correct to 0.1 pp, and the controls interval [43.8, 100.0] is
   **identical in Table 7 and Fig 7** (same z convention in both).
7. **F8 cells** vs Table 9 and `results/benchmark/baselines.json`: IDV3 T² 3 / SPE 5 (source
   0.0288/0.0475 = the table's 2.9%/4.8%); IDV4 49/100 (source 0.4875 = the table's 48.8%);
   IDV11 54/66; batch-93 15/74; controls 1/1; bare-LLM column = 9 "hit" + 2 "normal" + "n/a";
   FE-style column = 6/6 TEP hits. Magnified crops of the rendered figure confirm cell values
   (an initial low-resolution misread of "48/68" on my part dissolved at 2.4× magnification).

## 5. Claim-vs-reality spot-checks against repository ground truth (4 performed)

1. **Wilson z constant** — `paper/figures/make_figures.py:74` `def wilson(x, n, z=1.96)`
   ("matches the released scorer") vs `scripts/benchmark/aggregate.mjs:28`
   `function wilson(k, n, z = 1.96)`. **MATCH** — figures and released scorer share one constant;
   the briefing's required controls interval [43.8, 100.0] appears in Table 7 *and* Fig 7.
2. **Fig 5 case-study data** — `workspace/diagnostic-runs/202609150453290_bench_skab_valve1_1/
   02_processed/anomaly_segment_analysis.json`: `anomaly_window.index_range = [631, 919]`,
   `flow_in_window.switch_count_30_31 = 210`, `value_counts["30.0"] = 125`,
   `n_runs_at_30 = 106`, `mean_run_len = 1.18`, detection string carries "39.4% residence";
   `current_in_vs_out` = 0.947 / 0.9898 (figure prints 0.990); `04_diagnostics/confidence.json`
   `overall_confidence.score = 65`. Every annotation in Fig 5 is read from these files by
   `make_figures.py:410-468` (no hand-entered values). **MATCH.**
3. **Fig 3/4/8 verdict encodings** — `results/benchmark/gradings/*.json`: skab_valve1_1
   COMPETING_SET conf 65 topk=True → hatched sky "ranked"; skab_cavitation_13 CS 58 topk=True →
   hatched sky; tep_d03_hard CS 60 topk=False → amber "unranked"; indpensim_batch093 DETERMINED 88
   top1=True → blue; tep_d00_normal_control DETERMINED 88 → grey "normal 0.88"; rubric 85 /
   judge 90 for the valve row. All figure cells reproduce the released fields exactly. **MATCH.**
4. **Fig 8 headline cell** — `results/benchmark/metrics.json` `fault_cases=9, top1=6,
   control_pass=3`; `baselines.json` pca.tep_d04 `detection_rate_T2=0.4875, SPE=1`;
   `baselines.json` llm: 9/9 strict hits, FE-style 6/6, TEP control absent from the bare-LLM arm
   (→ the figure's "n/a", honestly captioned as a coverage gap). **MATCH.**

## 6. Float discipline & page makeup

- All 20 floats (8 figures, 12 tables) are **top-aligned** on their pages — `@fptop=0pt` works;
  no vertically-centred float pages, no figure ever appears below mid-page. Verified on every
  figure/table page listed above.
- `placeins [section]` confines floats to their sections; maximum first-citation-to-placement
  drift is ~2 pages (Figs 6/7 cited p44, placed p46/47), always inside the citing section and at a
  page top. Acceptable preprint behaviour, honestly pre-declared in the source comments.
- LaTeX log: 5 Overfull \hbox warnings, all ≤ 3.81 pt (largest: the Table 1 "FaultExplainer[1]"
  header cell) — invisible in the render; no text intrudes into margins. No unresolved "??" refs,
  no undefined citations.
- Stub pages: **p55** is the one genuine stub (five lines ending Section 8.5, then ~80% white,
  caused by Fig 8 flushing to its own top-aligned page p56). **p18** is a float page holding only
  Table 3 (~40% fill); p75 is the normal short final page. In a double-spaced `elsarticle` preprint
  these are tolerated; p55 is the only one a copy-editor would likely tighten.

## 7. Defect list

**Blocking: none.** The figure programme is internally consistent, caption-faithful, data-faithful
to the released artifacts, and print-clean.

Cosmetic / should-fix (non-blocking):
1. **p55** — five-line section-end stub before Fig 8's float page. Fix: let Fig 8 use `[!t]` on the
   same page as the section-end text by trimming its height, or accept as preprint typography.
2. **p18** — Table 3 alone on a ~40%-filled float page (and p17 similarly loose for Table 2).
   Fix: allow the two tables to share one float page or flow text behind them.
3. **Fig 3** — both panels define marker/bar semantics only in the caption (legends deliberately
   omitted). Fix: add compact inset legends; AEI readers should not need the caption to decode a
   panel.
4. **Fig 4** — hatched grey control bars vs hatched sky ranked-CS bars share the same '///' hatch
   at similar grayscale luminance. Fix: switch controls to a dot ('...') or 45°-reverse hatch.
5. **Fig 8** — IDV11 SPE cell (66) sets white text on mid-teal (threshold r > 0.62); lowest text
   contrast in the programme, still readable. Fix: raise the white-text threshold to ~0.55.
6. **Fig 7** — the 50% reference label sits slightly close to the o1-preview row band; nudge up
   2 pt for cleanliness (pure polish).

## 8. Bottom line

The figure programme is **print-clean for AEI**: eight caption-faithful figures and twelve
well-made tables, colour-blind-safe with luminance-safe hatching, no text below 7 pt printed,
vector/TrueType output, top-aligned floats, zero unresolved references, and — verified against the
released gradings, baselines, run artifacts, and the shared z = 1.96 Wilson scorer — not a single
number in any figure that contradicts the text, the tables, or the underlying data. The six
should-fix items are copy-editorial polish, none blocking.
