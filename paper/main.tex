\documentclass[preprint,review,12pt]{elsarticle}

\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\journal{Advanced Engineering Informatics}

\begin{document}

\begin{frontmatter}

\title{Trustworthy agentic diagnosis for industrial processes: evidence-graded competing-hypothesis reasoning with per-fault literature baselines on public benchmarks}

\author[a]{Anonymous Corresponding Author}
\ead{anon@example.com}
\address[a]{Industrial Deep Diagnostic Project}

\begin{abstract}
Large language model (LLM) based diagnosis promises root-cause analysis of industrial processes without task-specific model training, yet two obstacles keep it out of engineering practice: hallucinated conclusions that cannot be audited, and evaluation protocols that score sample-level classification while engineers need scenario-level, mechanism-grounded root causes. This paper presents the Industrial Deep Diagnostic (IDD) pipeline, an agentic diagnosis system coupling a nine-stage orchestration (ontology mapping, deterministic statistics, competing-hypothesis reasoning, automatic physical verification, quality gates with bounded repair) with evidence grading (levels L1--L7) and a four-condition anti-spurious-correlation filter. We further introduce a machine-checked evaluation framework---truth isolation enforced by an automated leakage sentinel, data fingerprinting, recorded inference traces, reproducibility gate---and apply it to a 12-scenario benchmark on three public datasets (SKAB, Tennessee Eastman Process, IndPenSim): 9 fault scenarios, each of the six TEP faults carrying a published per-fault baseline from FaultExplainer, and 3 normal controls. The benchmark exercises the \emph{reasoning stage} of the pipeline on pre-computed statistical evidence rather than the full agent orchestration, and we state the resulting scope precisely. IDD achieves Top-1 root-cause accuracy of 9/9 (Wilson 95\% CI [70.1\%, 100\%]), CDR\,=\,1.00, perfect calibration, and zero false alarms, with all 12 executions passing the finalisation gate. Per fault, IDD matches or exceeds both published single-LLM baselines without candidate lists---including IDV1, mis-diagnosed by the GPT-4o baseline, and the PCA-undetectable IDV3/IDV4 that FaultExplainer leaves unscored. The statistical stage is byte-reproducible and every grading is recomputable from the per-scenario records; the reasoning stage, like any LLM inference, is not bit-reproducible, and we report the two layers separately. The complete evidence repository is released for scrutiny.
\end{abstract}

\begin{keyword}
Fault root-cause analysis \sep Large language models \sep Multi-agent systems \sep Competing-hypothesis reasoning \sep Evidence grading \sep Reproducible benchmark
\MSC 68T05 \sep 62P30
\end{keyword}

\end{frontmatter}

\section{Introduction}
\label{sec:intro}

Root-cause analysis (RCA) of industrial process anomalies is a knowledge-intensive activity: the engineer must decide \emph{which} of many correlated sensor deviations represents the true underlying cause, justify that judgement with evidence, and communicate it with a calibrated degree of confidence. Three long-standing obstacles make automation of this task unsatisfying in practice.

\textbf{First, single-LLM diagnosis is unstructured and unauditable.} Recent evaluations of LLM-based fault explanation on the Tennessee Eastman Process (TEP)~\cite{khan2024faultexplainer} show that large models can articulate plausible mechanisms, but their answers arrive as free-form text with no machine-checkable structure, no separation of facts from inference, and no mechanism that prevents a confident-sounding conclusion from resting on a spurious correlation. A recent review of agentic approaches to process monitoring and fault diagnosis concludes that evidence for LLM/multi-agent fault diagnosis remains scarce~\cite{agentic2025review}: the direction is promising but lacks engineering-grade pipelines.

\textbf{Second, classical data-driven fault detection and diagnosis (FDD) optimises the wrong target.} Principal-component methods~\cite{goldenberg2019fault}, deep classifiers~\cite{tuli2022tranad,pozdnyakov2024adversarial}, and their benchmarks~\cite{rieth2023additional} score \emph{per-sample} detection or classification. An engineer, however, asks a \emph{per-episode} question: given this anomalous run, what is the root cause, how confident are we, and what evidence supports it? A 99\%-accurate classifier that returns a fault label without mechanism, evidence, or calibration does not answer that question. The detection--diagnosis gap is not merely theoretical: on TEP, PCA cannot detect four of the fifteen documented faults (IDV3, IDV4, IDV9, IDV15)~\cite{chiang2001fault}, and the published LLM baselines follow PCA in leaving these faults unscored~\cite{khan2024faultexplainer}.

\textbf{Third, correlation is not causation.} In recycled process loops almost every pair of variables is correlated through control action or material balance. An RCA method that treats strong correlation as causation will confidently report control-loop artefacts as root causes. Despite decades of causal-inference literature~\cite{katal2024causal}, contemporary LLM-diagnosis studies rarely include explicit anti-spurious safeguards.

This paper addresses the three obstacles with the Industrial Deep Diagnostic (IDD) pipeline and a benchmark built to evaluate it honestly. Our contributions are:

\begin{enumerate}
\item \textbf{A quality-gated agentic diagnosis pipeline} that couples deterministic statistical tooling with LLM reasoning under a competing-hypotheses protocol (Section~\ref{sec:method}), bounded repair loops, and automatic physical verification, so that every conclusion is decomposable into hypothesis, evidence, exclusion, and confidence.
\item \textbf{An evidence-grading and calibration scheme} (levels L1--L7 with gate propagation) that bounds reported confidence by the weakest supporting evidence level; on the benchmark corpus every verdict is correctly typed and \emph{zero} overconfident conclusions occur, including on faults whose statistical signature is deliberately weak (Section~\ref{sec:results}).
\item \textbf{A reproducible evaluation framework}---truth isolation, cryptographic data fingerprinting, recorded inference traces, and a machine-checked reproducibility gate---stress-tested by a \emph{delete-and-rebuild audit}: all results were removed and the chain re-executed from raw data, regenerating byte-identical statistical evidence and reproducing every grading (Section~\ref{sec:framework}).
\item \textbf{A 12-scenario benchmark} on three public datasets (SKAB, TEP, IndPenSim) whose six-fault TEP subset carries \emph{per-fault} published baselines from FaultExplainer~\cite{khan2024faultexplainer}. IDD reaches Top-1 accuracy of 100\% (Wilson 95\% CI $[70.1\%,100\%]$), CDR $=1.00$, and zero false alarms; per fault, it matches or exceeds both published single-LLM baselines---including IDV1, mis-diagnosed by the GPT-4o baseline, and the PCA-undetectable IDV3/IDV4---while using no candidate root-cause lists (Sections~\ref{sec:results}--\ref{sec:comparison}).
\end{enumerate}

Section~\ref{sec:related} positions the work against data-driven FDD, LLM-based diagnosis, and multi-agent orchestration research. Sections~\ref{sec:arch} and \ref{sec:method} detail the architecture and methodology. Section~\ref{sec:framework} defines the evaluation framework. Sections~\ref{sec:setup}--\ref{sec:comparison} present the benchmark and comparisons, and Section~\ref{sec:discussion} discusses limitations.

\section{Related work}
\label{sec:related}

\subsection{Data-driven fault detection and the benchmark gap}
Statistical process monitoring on TEP established the dominant evaluation template: PCA and projection methods with $T^2$/Q statistics~\cite{downs1993plant,goldenberg2019fault}, later extended by deep anomaly detectors~\cite{hartung2023deep,tuli2022tranad} and adversarial robustness studies~\cite{pozdnyakov2024adversarial}. These works score \emph{sample-level} detection or \emph{fault-class} accuracy. Critically, they do not evaluate root-cause \emph{mechanisms}: a classifier that labels a run ``IDV(4)'' provides the class identity but neither a causal mechanism, an evidence trail, nor a confidence statement. The classical detection ceiling itself is documented: among the 15 documented TEP disturbances, PCA misses IDV3, IDV4, IDV9 and IDV15~\cite{chiang2001fault}. Benchmark methodology has drawn scrutiny: Hendriks et al.~\cite{hendriks2022towards} and Vieira et al.~\cite{vieira2026towards} show that random segment splits inflate reported AUROC by up to 12.6 percentage points, motivating our entity-level scenario splits (Section~\ref{sec:framework}).

\subsection{LLM-based industrial diagnosis}
FaultExplainer~\cite{khan2024faultexplainer} is the closest published system: it couples PCA-based detection with LLM-generated fault explanations on TEP, reporting 7/11 (GPT-4o) and 9/11 (o1-preview) correct root causes among the 11 PCA-detectable faults, with root-cause candidates supplied in the prompt and alias matches accepted. Two properties of that protocol matter for comparison: (i) the candidate list converts the task from open diagnosis to ranked recognition; (ii) the four faults PCA cannot detect are excluded from scoring entirely. Our benchmark keeps both dimensions visible---per-fault outcomes on the shared faults, plus explicit scoring of two PCA-undetectable faults---under a strictly harder prompt regime (no candidates). Gong et al.~\cite{gong2026harnessing} orchestrate multiple LLM agents with difficulty-aware routing, but evaluate on a multiple-choice QA benchmark (FailureSensorIQ~\cite{constantinides2025failuresensoriq}) rather than on real time-series data. In Advanced Engineering Informatics, Liu et al.~\cite{liu2025probing} combine scene graphs, fine-tuned Llama-3 and augmented reality for machine-tool maintenance, and causal-graph RCA has been studied for time-varying processes~\cite{liu2025spatial} and knowledge-map-based group decision settings~\cite{yue2023root}; a label-free evaluation scheme for diagnosis models has been proposed in the same venue~\cite{liu2024label}. Table~\ref{tab:related} summarises the positioning: no published system combines (i) real time-series inputs, (ii) mechanism-level root causes with evidence trails, (iii) calibrated three-state conclusions, and (iv) a machine-verifiable evaluation contract.

\begin{table}[t]
\caption{Positioning against the closest published systems. ``Scenario-level RCA'' = one mechanism-grounded root-cause conclusion per anomalous run with evidence; ``calibrated'' = reported confidence bounded by evidence quality; ``repro.\ gate'' = machine-checked reproducibility contract.}
\label{tab:related}
\centering\small
\begin{tabular}{lccccc}
\toprule
System & Input & Scenario-level RCA & Evidence trail & Calibrated & Repro.\ gate \\
\midrule
FaultExplainer~\cite{khan2024faultexplainer} & TEP series & partial (LLM text) & partial & no & no \\
Gong et al.~\cite{gong2026harnessing} & MCQA only & no (QA accuracy) & no & no & no \\
PCA/Deep FDD~\cite{goldenberg2019fault,tuli2022tranad} & series & no (sample labels) & no & n/a & partial \\
Causal RCA~\cite{liu2025spatial,yue2023root} & series & partial (graph nodes) & partial & no & no \\
\textbf{IDD (this work)} & \textbf{series} & \textbf{yes (3-state)} & \textbf{yes (L1--L7)} & \textbf{yes} & \textbf{yes} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Multi-agent orchestration frameworks}
Generic agent frameworks (AutoGen, CrewAI and derivatives) provide message-passing and role prompting but leave audit, evidence policy, and evaluation contracts to the application. Our position, following the practice of the reviewed agentic-FDD literature~\cite{agentic2025review}, is that industrial diagnosis requires these as \emph{first-class, machine-checked} pipeline elements rather than prompt conventions; Section~\ref{sec:arch} describes how IDD enforces them through checkpoints, bounded repair loops, and an event-log execution proof.

\section{System architecture}
\label{sec:arch}

\subsection{Pipeline overview}
IDD organises diagnosis as a nine-stage pipeline executed by 14 specialised agents backed by 18 skill packages (Fig.~\ref{fig:pipeline}). The main orchestrator performs setup and data ingestion (Step~0/1) with adaptive preprocessing; a context-builder agent constructs a process ontology with variable semantics, physical principles, and confounder annotations (Step~2, checkpoint CP-2/3); a data-processor agent runs the deterministic statistical stage---per-variable anomaly profiling, cross-domain correlation, lag-compensated cross-correlation, batch integrity checks---producing a machine-readable analysis conclusion (Step~3, CP-4); a VLM agent reads rendered charts as visual evidence (Step~3.5). The diagnostician agent then performs competing-hypothesis reasoning over the statistical and ontological evidence (Step~4, CP-5). Two parallel auditors---a ten-criterion judge and a physical pre-auditor---gate the conclusion (Step~5a/5b); the reporter assembles a pyramid-structured report (Step~6); a final physical audit must return ENDORSED before the HTML visualisation and review (Steps~8/8.5) and the evidence-closure finalisation (Step~9, CP-9).

\begin{figure}[t]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig_pipeline.png}
\caption{IDD diagnosis pipeline (v6.5). Deterministic statistical tooling (Steps 0--3) feeds LLM reasoning under a competing-hypotheses protocol (Step 4); parallel judge and physics pre-audit gates (5a/5b) may trigger bounded repair loops ($\le$3 per fault, global cap 5, anti-oscillation confidence cap 0.50); a final physics audit (Step 7) must return ENDORSED before delivery, and the finalisation gate (CP-9) aggregates inventory, schema, closure, judge-cross-audit, and event-log checks into a single PASS/FAIL verdict. Every transition is appended to a machine-verifiable event log.}
\label{fig:pipeline}
\end{figure}

\subsection{Execution layer: interchangeable engines with a uniform contract}
Reasoning agents are executed by any of fourteen interchangeable engines (Claude Agent SDK, OMP RPC, Codex app-server, DeepSeek/Hermes/Qwen ACP dialects, OpenCode, and one-shot CLIs including Gemini CLI, Copilot CLI, Cursor CLI, Crush, Goose, and pi) behind a single contract (\texttt{startDiagnosis/startSessionChat/parseStreamEvent/registerChild/closeQuery}). Engine availability is probed and validated before execution with a three-state contract (unknown engine \texttt{400}, unusable engine \texttt{409}); a scripted in-process mock engine allows the full pipeline to be exercised in CI without external dependencies. Because every engine emits the same standardised event stream, engine choice is a deployment rather than a methodological variable, and engine substitution is itself a supported ablation axis.

\subsection{Scope of the evaluated configuration}
\label{sec:scope}
The benchmark of Sections~\ref{sec:setup}--\ref{sec:comparison} measures the pipeline's \emph{reasoning stage}, not the full agent orchestration, and we state this precisely because the distinction bounds every performance claim in this paper.

In the evaluated configuration the deterministic stages (data ingestion, statistical profiling, brief generation) run as versioned scripts, and the diagnostic reasoning is performed by an LLM agent that receives a pre-computed statistical brief and returns a structured note conforming to the diagnostician protocol (competing hypotheses with mechanism classes, evidence with levels, elimination, three-state conclusion). The note is then expanded into the full schema-compliant artifact set (ontology, diagnosis, evidence, confidence, reasoning chain, review), the pipeline's own gates are executed, and only then is the scenario graded. The evaluated element is therefore the \emph{evidence$\rightarrow$conclusion reasoning step under the protocol}, which is the step the paper's methodological claims concern; the orchestrator, the parallel judge and physics pre-audit, and the bounded repair loop are architecture contributions that are exercised in deployment runs but are \emph{not} separately ablated by this benchmark. Where a scenario's gate outcome is reported, it reflects the gate check on the expanded artifact set.

Two consequences are worth stating plainly. First, the headline accuracies are not accuracies of the fourteen-agent orchestration as a whole. Second, the quality-gate score reported per scenario is the value recorded by the diagnosing agent in its own structured note; it is \emph{not} an independently computed judge verdict. This is not a cosmetic caveat: in the released artifacts all ten criteria of the gate carry the identical value, because the expansion step set every criterion to the note's own score divided by ten. Across all twelve runs every criterion scored 9, so the gate carried no discriminative information and its reported mean merely restated what each graded artifact said about itself. We therefore do not rest any conclusion on it. To provide a genuine substitute we added an independent structural audit that inspects the emitted artifacts rather than the note, verifying for each criterion that the evidence it depends on is actually present, traceable and non-degenerate (evidence levels and sources, confidence bounds and ceilings, falsification and revival conditions, the anti-spurious-correlation block, three-state consistency). All twelve runs pass it. We state its limit as clearly as its result: it is a \emph{floor}, not a ranking — it establishes that the work was really performed, not that a conclusion is correct, and a grader that distinguishes good reasoning from mediocre reasoning remains future work.

Finally, terminological care is required when reading the control results. The protocol's \textsc{determined} type means that a definite conclusion was reached; in three control scenarios the definite conclusion is that the plant was running normally. A \textsc{determined} control therefore does \emph{not} mean a fault was identified, and we report control performance through the separate no-false-alarm criterion rather than through the fault counts.

\subsection{Audit infrastructure}
Every stage transition appends a structured event to \texttt{.pipeline\_events.jsonl}; a log checker validates agent starts/completions against the required contract, and a finalisation gate aggregates inventory, schema, closure, judge-cross-audit, and event-archive checks into a single PASS/FAIL verdict. A run that fails any critical check cannot be graded, which is the property the benchmark relies on: \emph{no result exists without an execution proof}.

\section{Diagnostic methodology}
\label{sec:method}

\subsection{Competing-hypotheses protocol}
The diagnostician agent must generate at least three candidate hypotheses per fault scenario, assign each a mechanism class (e.g., fluid restriction, cavitation, rotor dynamics, feed-composition step, cooling disturbance, sensor drift), collect for each an evidence list with explicit levels, identify contradictions, and eliminate hypotheses whose contradictions are stronger than their support. The protocol admits exactly three conclusion states: \textsc{determined} (a single surviving hypothesis), \textsc{competing\_set} ($\ge$2 survivors that the data cannot discriminate, with confidence capped at 0.70 and the discriminating measurement named), and \textsc{needs\_data} (evidence insufficient; missing data itemised). A fourth, implicit state---normal operation with no confirmed fault---applies to control scenarios. The elimination contract is quantitative: every elimination must cite the contradicting evidence, and every surviving chain must cite evidence numbers present in the statistical digests.

\subsection{Evidence grading and gate propagation}
Every piece of evidence carries a level L1--L7 (L1 direct measurement, L2 operating documents, L3 statistical analysis, L4 visual evidence, L5 domain ontology/mechanism, L6 external reference, L7 unsupported assumption). The scenario confidence is bounded by the weakest level contributing to the surviving chain, and the quality gate rejects conclusions whose confidence exceeds that bound. This converts ``hallucination control'' from a prompt instruction into a checkable arithmetic constraint.

\subsection{Anti-spurious-correlation filter}
Four conditions must hold before a correlation may be cited as causal support: (i)~temporal precedence of cause over effect, verified by lag-compensated cross-correlation; (ii)~statistical significance surviving detrending, Simpson-stratification checks, and multiple-testing correction; (iii)~a physical mechanism annotated in the ontology connecting the variables; and (iv)~absence of contradictions under leave-one-out outlier sensitivity analysis, with production-state (steady/batch-phase) filtering applied before correlation. Correlations failing the filter are recorded but explicitly barred from hypothesis support; definitional pairs (e.g., valve position and the level it controls, $r\!=\!1.0$) are classified as control-loop artefacts. In the benchmark this filter is what prevents control-loop correlations from being reported as root causes, and what allows normal batches with impulsive recipe-dosing spikes ($|z|$ up to 16.2) to be graded as healthy rather than faulty.

\subsection{Automatic physical verification}
After the conclusion is drafted, a physics checker recomputes quantitative feasibility (mass/energy balances, thermal and vibration magnitude envelopes) from the cleaned data, ontology, and anomaly report, producing a machine-readable \texttt{physical\_checks} verdict. When no automated check applies to a scenario's column semantics, the pipeline falls back to an explicit L1--L5 manual-verification memo; silent omission is not permitted.

\subsection{Quality gates, repair loops, and calibration}
A ten-criterion judge scores the diagnosis (threshold 90); a parallel physical pre-audit reviews mechanism chains; failures trigger bounded repair (per-fault rerun $\le$3, global cap 5, with anti-oscillation confidence capping at 0.50). The final physical audit must return ENDORSED. Confidence calibration is then auditable end-to-end: every scenario grading records whether the reported confidence respects the evidence ceiling, and \emph{overconfidence} is a first-class error metric.

\section{A reproducible evaluation framework}
\label{sec:framework}

IDD's benchmark framework (released as the project's \texttt{experience/} evidence repository and \texttt{docs/benchmark/} protocol) is designed around five contracts, each machine-enforced:

\textbf{(C1) Truth isolation.} Scenario definitions carry ground truth and grading keywords, but the pipeline's visible inputs contain only the process description and the data. Ground truth is confined to the grader. The diagnosing agent reasons from an automatically generated \emph{evidence brief} containing statistical digests and a process description that is restricted, by construction, to the medium, equipment topology, column semantics, sampling rate and anomaly window---never the fault identity. This restriction is not left to review: a leakage sentinel (\texttt{check-leakage.mjs}) mechanically rejects any scenario whose description contains fault vocabulary or grading keywords, any brief carrying grader-only keys, and any inference note that reproduces a dataset's own label wording verbatim. The sentinel gates the benchmark run and currently passes for all 12 scenarios. We report this as a design contribution precisely because the restriction proved non-obvious: an earlier revision of five of the six TEP descriptions disclosed the fault, which the sentinel now makes impossible to reintroduce silently.

\textbf{(C2) Data fingerprinting.} Every input file is hashed (SHA-256) into a dataset manifest (153 entries); the reproducibility gate re-verifies all fingerprints before any metric is accepted.

\textbf{(C3) Recorded inference.} The single LLM-dependent step---the diagnostic note---is written as a structured artifact with a fixed schema (hypotheses with mechanism classes, logic chains, evidence with levels, contradictions, falsification conditions, proof statements). Notes that omit required fields cannot be committed.

\textbf{(C4) Gate-graded scoring.} Committing a note expands it into schema-compliant artifacts (ontology, diagnosis, evidence, confidence, reasoning chain, review), runs the pipeline's own gates (log check, finalisation), and only then grades the scenario against ground truth. A scenario whose pipeline fails its gates is \emph{not graded}---it is a pipeline failure, not a benchmark result.

\textbf{(C5) Reproducibility gate.} A verifier recomputes all aggregate metrics from the per-scenario gradings, re-checks dataset hashes, verifies case coverage, and requires an execution proof per run; any drift fails the evaluation. The released report carries the resulting \texttt{REPRODUCIBLE} verdict alongside 153 verified fingerprints and 12 execution proofs.

\textbf{Delete-and-rebuild audit.} To test whether the released numbers are regenerable rather than curated, we re-executed the chain from the raw data after removing the derived artifacts. The statistical stage is deterministic, and regenerating the evidence briefs from the same inputs reproduced them byte-identically (SHA-256 equality across all twelve). Aggregate metrics were independently recomputed from the per-scenario grading records with zero drift, and the reproducibility gate re-verified all 153 dataset fingerprints and all twelve execution proofs. We separate two claims that are easy to conflate: \emph{the statistical layer is byte-reproducible} (verified), whereas \emph{the reasoning layer is stochastic}---the diagnostic notes are LLM-authored and are not guaranteed to be regenerated identically. The audit therefore establishes reproducibility of the evidence and the scoring chain, not determinism of the inference.

Scenario splits follow the entity-level discipline recommended by recent benchmark studies~\cite{vieira2026towards,hendriks2022towards}: each experimental batch/file appears in exactly one scenario, controls are paired with faults per dataset, and no segment-level splitting is used anywhere.

\section{Experimental setup}
\label{sec:setup}

\subsection{Datasets and scenarios}
We assemble 12 scenarios from three public benchmarks (Table~\ref{tab:bench}): \textbf{SKAB}~\cite{skab2020}, a closed water-circulation testbed with vibration, current, pressure, temperature, and flow channels at 1\,Hz---two fault scenarios (inlet-valve throttling; two-phase-flow cavitation drawn from the ``other'' group whose fault label is withheld from the scenario description) plus the anomaly-free control; \textbf{TEP}~\cite{downs1993plant,rieth2023additional}, with six documented fault scenarios---IDV1 feed-composition step, IDV3 D-feed-temperature step (classically hard to detect), IDV4 reactor-cooling-water step, IDV7 C-header pressure loss, IDV11 random cooling-water variation, IDV14 cooling-valve stiction---and the fault-free baseline as control; and \textbf{IndPenSim}~\cite{indpensim2020}, a 100{,}000\,L penicillin fermentation simulator, with one documented process-deviation batch (93) and one recipe-driven normal batch as control. Ground truth derives from verifiable sources only: SKAB's official label file, the established TEP cause table~\cite{chiang2001fault}, and IndPenSim's faulted-batch markers.

Three design decisions deserve emphasis. First, the TEP subset is chosen so that \emph{every fault carries a published per-fault baseline} from FaultExplainer~\cite{khan2024faultexplainer}, enabling scenario-granular comparison rather than aggregate-only comparison. Second, IDV3 and IDV4 belong to the set PCA cannot detect~\cite{chiang2001fault}---FaultExplainer leaves them unscored---so they probe root-cause reasoning \emph{beyond the classical detection limit}. Third, IDV4, IDV11 and IDV14 perturb the same physical channel (reactor cooling water) with three different dynamic forms (step, random variation, valve stiction); separating them requires reading the statistics of each recording, not recalling a fault label, which partially mitigates prompt-knowledge contamination.

\begin{table}[t]
\caption{Benchmark composition. The TEP subset carries per-fault published baselines (FaultExplainer, arXiv:2412.14492); IDV3/IDV4 are PCA-undetectable~\cite{chiang2001fault}.}
\label{tab:bench}
\centering\footnotesize
\begin{tabular}{llccp{6.4cm}}
\toprule
Dataset & Scenarios & Fault & Control & Truth source \\
\midrule
SKAB & 3 & 2 & 1 & official \texttt{ground\_truth.json} (fault label withheld for the ``other''-group case) \\
TEP & 7 & 6 & 1 & Downs \& Vogel / Chiang et al.~\cite{chiang2001fault} \\
IndPenSim & 2 & 1 & 1 & faulted-batch markers (batch 93) \\
\midrule
Total & 12 & 9 & 3 & --- \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Metrics}
For fault scenarios we report \textbf{Top-1} (primary root cause identified), \textbf{Top-k} ($k{=}3$, correct cause among candidates), \textbf{CDR} (Top-1 \emph{and} \textsc{determined}), \textbf{calibration} (reported confidence within the evidence ceiling; overconfidence $=$ \textsc{determined} \emph{and} wrong), and the pipeline's own \textbf{judge score} (ten-criterion quality gate, threshold 90). For control scenarios we report \textbf{pass} (no confirmed fault) and \textbf{false alarms}. Proportions carry Wilson 95\% confidence intervals. We deliberately do not report sample-level F1/AUC: they answer a different question and are used only as reference context (comparison protocol below).

\subsection{Comparison protocol}
We distinguish two comparison registers. \textbf{Register A (same-task direct comparison)}: methods that output one root-cause conclusion per scenario on the same dataset family; the published FaultExplainer per-fault numbers fall here, at scenario granularity. \textbf{Register B (reference only)}: sample-level detection/classification results; they are reported for context and must not be ranked against our scenario-level metrics. Two protocol differences are stated wherever Register A is invoked: FaultExplainer supplies candidate root causes in its prompt and accepts alias matches, whereas IDD uses no candidates and scores mechanism keywords strictly; and FaultExplainer scores only the 11 PCA-detectable faults, whereas IDD additionally scores PCA-undetectable IDV3/IDV4. Because the scenario sets differ, no significance test is claimed across systems.

\subsection{Implementation}
The deterministic stages run as versioned scripts (streaming CSV inspection, anomaly profiling, correlation with lag-compensated CCF, validation with Simpson/detrending/outlier-sensitivity/multiple-testing checks, physics checking via the shared scientific Python environment). The reasoning stage uses a frontier LLM through the engine layer (Section~\ref{sec:arch}); all prompts and their statistical evidence are recorded per scenario. A full 12-scenario sweep executes the deterministic stage in about 25\,s per scenario and the reasoning stage in minutes per scenario; the complete chain (prepare, briefs, diagnosis, gates, grading, aggregation, report) is driven by a single entry point with fail-fast stage gates.

\section{Results}
\label{sec:results}

\subsection{Overall performance}
Table~\ref{tab:overall} reports the headline numbers. IDD identifies the primary root cause in \textbf{9/9 fault scenarios} (Top-1 $=$ 100\%, Wilson 95\% CI $[70.1\%,100\%]$), with all 9 also correct within the top-3, CDR $=1.00$, and all 9 conclusions correctly typed as \textsc{determined}. Every reported confidence lies within the evidence-level ceiling (\textbf{calibration 9/9, overconfidence 0}), and every one of the 3 control scenarios is correctly passed with \textbf{zero false alarms}. All 12 pipeline executions pass the finalisation gate with valid event logs; the mean judge score is 91.0/100 (range 90--92). Fig.~\ref{fig:benchmark} summarises composition, per-dataset outcomes, and the decisive statistical signature of each fault scenario as recorded in the digests.

\begin{table}[t]
\caption{Overall results on the 12-scenario benchmark. Proportions carry Wilson 95\% CIs. These figures describe the reasoning stage under the protocol of Section~\ref{sec:scope}.}
\label{tab:overall}
\centering\small
\begin{tabular}{lcc}
\toprule
Metric & Value & 95\% CI \\
\midrule
Top-1 root cause (fault scenarios) & 9/9 $=$ 100\% & [70.1, 100.0] \\
Top-3 root cause (fault scenarios) & 9/9 $=$ 100\% & [70.1, 100.0] \\
CDR (Top-1 and \textsc{determined}; a rate) & 1.00 (9/9) & [70.1, 100.0] \\
Confidence calibration & 9/9 & --- \\
Overconfidence & 0/9 & --- \\
Control pass (no false alarm) & 3/3 & --- \\
Mean judge score (10-criterion gate) & 91.0/100 & \emph{agent self-reported} \\
Pipeline finalisation gate & 12/12 PASS & --- \\
Reproducibility gate & REPRODUCIBLE & --- \\
\bottomrule
\end{tabular}

\vspace{2pt}
\footnotesize Note: CDR is a \emph{rate} over the nine fault scenarios, not a count. The mean judge score is the value the diagnosing agent recorded in its own note (Section~\ref{sec:scope}) and is reported for completeness only; no claim in this paper depends on it. The reproducibility gate certifies the deterministic statistical layer and the scoring chain (Section~\ref{sec:framework}), not bit-reproducibility of LLM inference.
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig_benchmark.png}
\caption{Benchmark composition and results. Left: per-dataset scenario counts and truth sources; all 9 fault scenarios are Top-1 correct and all 3 controls pass without false alarms. Right: the decisive statistical signature per fault scenario as recorded in the digests, with per-scenario judge scores. Bottom badges: pipeline and reproducibility gate outcomes.}
\label{fig:benchmark}
\end{figure}

\subsection{Per-mechanism breakdown and within-family discrimination}
Table~\ref{tab:mech} lists the nine fault scenarios grouped by mechanism, with the decisive signature each diagnosis rested on. Three observations deserve emphasis.

\textbf{Within-family form discrimination.} IDV4 (step), IDV11 (random variation) and IDV14 (valve stiction) all perturb the same physical channel---reactor cooling water---yet produce three distinct statistical forms that the pipeline separated correctly: a step-dominant plateau (reactor temperature $z{=}10.06$, $2.6\times$ the runner-up), sporadic weak deviation ($z{=}3.73$, no plateau), and a control-loop stress signature (temperature--valve pair $r{=}0.998$ with no plateau). Recalling the textbook label ``cooling-water fault'' cannot produce these three different diagnoses; only the per-recording statistics can. This is our principal mitigation against prompt-knowledge contamination (Section~\ref{sec:discussion}).

\textbf{Reasoning beyond the detection limit.} IDV3 and IDV4 belong to the four-fault set that PCA cannot detect~\cite{chiang2001fault}; published LLM baselines inherit this limitation and leave them unscored~\cite{khan2024faultexplainer}. IDD correctly diagnosed both---by exclusion chains over the surviving mechanism families rather than by a detection statistic.

\textbf{Blind-set robustness.} The SKAB cavitation scenario is drawn from the ``other'' experiment group whose fault label is withheld from the scenario description; the pipeline identified cavitation from the signature alone (vibration--flow \emph{negative} correlation $r{=}{-}0.821$ with pressure instability $z{=}5.02$).

\begin{table}[t]
\caption{Per-scenario results with the decisive statistical signature (all values transcribed from the scenario digests; full evidence chains in the evidence repository). ``Judge'' is the agent's self-reported gate score (Section~\ref{sec:scope}), shown for completeness and not used as evidence.}
\label{tab:mech}
\centering\footnotesize
\begin{tabular}{lccp{6.2cm}}
\toprule
Scenario (mechanism) & Conclusion & Judge* & Decisive signature \\
\midrule
SKAB inlet-valve throttling & \textsc{determined} 0.87 & 92 & line-side vibration burst $A2$ $z{=}13.56$; electrical channels normal \\
SKAB cavitation (blind set) & \textsc{determined} 0.82 & 92 & vib--flow negative corr.\ $r{=}{-}0.821$; pressure $z{=}5.02$ \\
TEP IDV1 A/C feed-ratio step & \textsc{determined} 0.84 & 92 & component spectra dominate 4/6 columns ($z{\approx}4.9$) \\
TEP IDV3 D-feed temp.\ (hard) & \textsc{determined} 0.72 & 90 & weak-signature exclusion chain (tower heat balance + feed link) \\
TEP IDV4 cooling temp.\ (step) & \textsc{determined} 0.86 & 92 & reactor temp.\ step-dominant $z{=}10.06$ \\
TEP IDV7 C-header pressure & \textsc{determined} 0.80 & 91 & F-family shift across streams 6/9/11 + dual pressure $z{\approx}4.6$ \\
TEP IDV11 cooling temp.\ (random) & \textsc{determined} 0.70 & 90 & sporadic $z{=}3.73$ form vs.\ $10.06$ step reference \\
TEP IDV14 valve stiction & \textsc{determined} 0.68 & 90 & loop pair $r{=}0.998$ (temp--valve), no plateau \\
IndPenSim batch-93 deviation & \textsc{determined} 0.85 & 91 & multi-actuator sync (acid $z{=}15.8$, heater $13.05$) + pH $7.09$ \\
\midrule
Total & 9 \textsc{determined} & mean 91.0 & --- \\
\bottomrule
\end{tabular}

\vspace{2pt}
\footnotesize *Agent self-reported, not independently scored.
\end{table}

\subsection{Calibration and false-alarm suppression}
Fig.~\ref{fig:calib} plots the reported confidence per fault scenario. Reported confidences span 0.68--0.87; the three weakest-signature scenarios (IDV14 stiction 0.68, IDV11 random 0.70, IDV3 hard case 0.72) are self-limited to the protocol band and carry explicit falsification conditions naming the measurement that would revise them---\emph{overconfidence} $=$ \textsc{determined}-and-wrong never occurs. Control scenarios demonstrate the complementary discipline: normal IndPenSim batches contain recipe-dosing impulses reaching $|z|{=}16.2$, which the pipeline classifies as operational form (sparse recipe pulses with intact mass-balance, respiration-coherence, and product-accumulation invariants) rather than faults; the normal TEP run exhibits control-loop correlations at $|r|{=}1.0$, which the anti-spurious filter classifies as definitional. These are precisely the failure modes that produce false alarms in naive correlation-driven diagnosis.

\subsection{Numerical robustness and the silent-degradation failure mode}
\label{sec:degradation}
Reproducing the pipeline on the IndPenSim batch exposed two defects that are worth reporting because they are generic to statistical diagnosis pipelines, and because both were \emph{invisible} in the released artifacts until the underlying data were re-analysed.

The first is a numerical-guard defect. The Pearson coefficient was computed in the textbook single-pass form, whose denominator is the square root of a product of two differences of large nearly-equal sums. On a near-constant channel, catastrophic cancellation made that product marginally negative, so the square root raised a domain error and aborted the entire statistics run. The second is a serialisation defect: a skewness statistic divided the mean by the median, and driver channels such as acid flow rate sit at zero for most of a batch, producing an infinite ratio that was written as the bare token \texttt{Infinity}. That token is not legal JSON, so the emitted report was unreadable to every strict parser (browser/Node \texttt{JSON.parse}, \texttt{jq}, Go, Rust) even though Python's lenient reader accepted it. We fixed both: a non-positive discriminant now yields no correlation instead of an exception, and non-finite statistics are mapped to null at the serialisation boundary with a count reported.

The engineering significance lies not in either fix but in how they surfaced. The domain error caused the statistics stage to fail, whereupon the driver fell back to a reduced JavaScript path that computes univariate summaries and \emph{omits the entire anti-spurious-correlation layer}. The affected run still passed finalisation, still received a quality-gate score, and still counted in the aggregate; nothing in the reported metrics distinguished it from a fully analysed run. We found it only by auditing the artifacts directly. A degraded run is not merely less complete: it silently removes the confound-control evidence that Section~\ref{sec:method} identifies as the pipeline's main defence against spurious causal claims, so its conclusion is not comparable with the others. We therefore recommend, and have implemented for this benchmark, that the statistics engine identity be carried into the aggregate reporting so that a fallback run is visible rather than merely inferable (all twelve scenarios now execute the full statistics package). Treating an analysis-layer degradation as a first-class, gate-failing condition, rather than a warning, is the natural next step.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig_calibration.png}
\caption{Reported confidence per fault scenario in the blind re-diagnosis run. Amber bars mark weak-signature scenarios whose confidence is self-limited to 0.68--0.72 under the evidence-ceiling rule; no conclusion exceeded the ceiling implied by its evidence levels (calibration 9/9, overconfidence 0).}
\label{fig:calib}
\end{figure}

\section{Comparison with published baselines}
\label{sec:comparison}

\subsection{Register A: per-fault comparison on TEP}
FaultExplainer~\cite{khan2024faultexplainer} reports Top-1 root-cause correctness of 7/11 (63.6\%, Wilson CI $[35.4\%,84.8\%]$) for GPT-4o and 9/11 (81.8\%, CI $[52.3\%,94.9\%]$) for o1-preview, on the 11 PCA-detectable TEP faults, with root-cause candidates included in the prompt and alias matches accepted. IDD, without candidate lists, is correct on \textbf{6/6} faults of its TEP subset (Wilson CI $[61.0\%,100\%]$). Fig.~\ref{fig:perfault} gives the per-fault breakdown against the transcribed FaultExplainer Table~1 outcomes and PCA detectability; Fig.~\ref{fig:forest} visualises the aggregate point estimates with their intervals. Three per-fault outcomes stand out:

\begin{itemize}
\item \textbf{IDV1 (A/C feed-ratio step):} the GPT-4o baseline mis-diagnoses this fault, attributing the top deviations to an alternative mechanism; IDD identifies the feed-composition step from the component-channel signature (four of the six most deviant columns are composition measurements across streams 6/9/11).
\item \textbf{IDV3 and IDV4 (PCA-undetectable):} both faults are excluded from FaultExplainer's scoring because PCA cannot detect them; IDD scores and solves both---IDV4 from the step-dominant reactor-temperature signature, IDV3 by an exclusion chain over the surviving families under an intentionally weak signature.
\item \textbf{IDV7, IDV11, IDV14:} both systems are correct; IDD additionally separates the three cooling-water perturbation \emph{forms} (Section~\ref{sec:results}), a granularity below fault-identity level.
\end{itemize}

Because the scenario sets and prompt regimes differ (candidates vs.\ none; alias acceptance vs.\ strict keyword scoring), we refrain from a formal significance test and present the per-fault table as the primary evidence; on matched faults IDD is never wrong where a baseline is right, and is additionally right on three fault--condition cells (IDV1 under GPT-4o; IDV3/IDV4 unscored by both baselines) that the baselines do not cover.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\textwidth]{figures/fig_tep_perfault.png}
\caption{Per-fault comparison on the TEP subset: IDD vs.\ FaultExplainer (transcribed Table~1 outcomes, arXiv:2412.14492) and PCA detectability (Chiang et al.~\cite{chiang2001fault}). IDD is correct on 6/6 without candidate lists, including the PCA-undetectable IDV3/IDV4 that FaultExplainer leaves unscored and IDV1 that the GPT-4o baseline mis-diagnoses.}
\label{fig:perfault}
\end{figure}

\begin{table}[t]
\caption{Per-fault outcomes on the TEP subset (machine-readable companion to Fig.~\ref{fig:perfault}). FE = FaultExplainer (arXiv:2412.14492, root-causes-included prompt, alias hits accepted); ``unscored'' = excluded from FE scoring because PCA cannot detect the fault~\cite{chiang2001fault}. IDD: strict mechanism-keyword scoring, no candidate list.}
\label{tab:perfault}
\centering\footnotesize
\begin{tabular}{llccl}
\toprule
TEP fault & FE GPT-4o & FE o1-preview & PCA detectable & IDD Top-1 \\
\midrule
IDV1 A/C feed-ratio step (Stream 4) & wrong & correct & yes & \textbf{correct} \\
IDV3 D-feed temperature step & unscored & unscored & \textbf{no} & \textbf{correct} \\
IDV4 reactor cooling-water temp.\ (step) & unscored & unscored & \textbf{no} & \textbf{correct} \\
IDV7 C-header pressure (Stream 4) & correct & correct & yes & \textbf{correct} \\
IDV11 cooling-water temp.\ (random) & correct & correct & yes & \textbf{correct} \\
IDV14 cooling-water valve (stiction) & correct & correct & yes & \textbf{correct} \\
\midrule
Scoreable-set summary & 7/11 & 9/11 & --- & 6/6 (CI $[61.0,100]$) \\
\bottomrule
\end{tabular}

\vspace{2pt}
\footnotesize Note on comparability: the FE columns and the IDD column are \emph{not} values of a common metric. FE scores fault-class identification with candidate root causes supplied in the prompt and alias matches accepted, over the 11 PCA-detectable faults; IDD scores strict mechanism-keyword identification with no candidates, over its six-fault TEP subset. The columns are placed side by side to expose per-fault agreement and disagreement, not to support subtraction, ranking, or a significance test; consistent with this, we report no test statistic. The PCA column is the published detectability result~\cite{chiang2001fault} rather than a local reproduction.
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\textwidth]{figures/fig_forest.png}
\caption{Register-A aggregate comparison with Wilson 95\% intervals. Grey: FaultExplainer-reported single-LLM baselines ($n{=}11$, candidates in prompt). Blue: IDD (no candidates). Sets differ, so overlapping intervals are expected and no significance test is claimed; the per-fault table (Fig.~\ref{fig:perfault}) is the finer-grained evidence.}
\label{fig:forest}
\end{figure}

\subsection{Register B: reference-only context}
For context we report sample-level results from the literature without ranking: TEP classification accuracies of 0.887--0.907 (MLP/GRU/TCN~\cite{pozdnyakov2024adversarial}), anomaly-detection F1 of 0.970 (BeatGAN~\cite{hartung2023deep}), SKAB ensemble F1/AUC of 0.85/0.88~\cite{iliopoulos2023detection}, and leakage-controlled bearing AUROC of 0.75--0.93~\cite{vieira2026towards}. These systems answer ``is this sample anomalous / which class''; IDD answers ``what is the mechanism, how confident, with which evidence''. The dimensions are complementary: a deployment would plausibly chain a sample-level detector (first-stage alarm) with a scenario-level RCA system (this work).

\subsection{Case study: an auditable evidence chain}
For the SKAB cavitation scenario the pipeline's recorded chain reads: (i)~L3 evidence---pressure $z{=}5.02$ with vibration--flow \emph{negative} correlation $r{=}{-}0.821$ (digest values); (ii)~L5 mechanism---entrained air at the pump inlet collapses on the impeller pressure side, and lower flow implies higher gas fraction, hence stronger excitation; (iii)~eliminations---valve throttling rejected (that signature is single-channel vibration dominance without the negative coupling), rotor anomaly rejected (electrical channels at $z{\le}2.82$), sensor drift rejected (multi-channel coherent dynamics cannot be produced by independent drifts); (iv)~conclusion---\textsc{determined}, confidence 0.82. The full chain, together with the physics-check verdict and the judge's ten-criterion scores (92/100), is persisted in the grading record---the unit of publication in our evaluation contract.

\section{Discussion and limitations}
\label{sec:discussion}

\textbf{Statistical power.} With $n{=}9$ fault scenarios, Top-1 $=100\%$ supports the claim ``$\ge$70.1\% at 95\% confidence'', not universal competence. We consider the per-fault evidence more informative than the aggregate: six of nine faults carry published per-fault baselines, and IDD is never wrong where a baseline is right while additionally covering the PCA-undetectable IDV3/IDV4. Extending the corpus (P2: the full 20-class TEP catalogue with the extended simulation dataset~\cite{reinartz2021extended}; bearing datasets; novel-disturbance suites) is the designed path to tighter intervals and paired tests.

\textbf{Prompt-knowledge contamination and shortcut learning.} TEP's documented fault mechanisms appear in textbooks and are therefore plausibly memorised by frontier models, and a benchmark that names the fault in its own inputs would measure recall rather than reasoning. Our protocol addresses this at two levels. First, the inputs are now mechanically constrained to withhold the fault identity: a leakage sentinel (Section~\ref{sec:framework}, C1) rejects any scenario whose process description contains fault vocabulary or grading keywords, and it gates the run; an earlier revision of five TEP descriptions disclosed the fault and was corrected before the current results. Second, even with the label withheld, knowing ``cooling-water fault'' cannot distinguish a step from a random variation from a sticking valve---the three IDV4/IDV11/IDV14 recordings perturb the same physical channel and require per-recording statistical form discrimination---and IDV3's signature is deliberately weak. The SKAB ``other''-group scenario removes the label entirely. We regard the sentinel as necessary but not sufficient: a novel-disturbance suite (combination perturbations absent from the literature) and an injection study that scores a deliberately label-free but mechanism-free answer are the definitive controls, and are under construction. Until they are reported, the TEP per-fault comparison in Section~\ref{sec:comparison} should be read as consistent with genuine per-recording discrimination rather than as proof of it.

\textbf{Scoring validity.} The three-state and calibration metrics are computed by the grader from the pipeline's typed output, but the root-cause verdict is a keyword decision, and a keyword decision is only as good as its keyword set: terms that also appear in a generic description of the process would be satisfied by any plausible answer. Two mitigations are in place---the leakage sentinel rejects keyword sets that are entirely generic, and a discrimination test injects a plausible-but-wrong diagnosis and requires the grader to reject it---but the discrimination test currently covers a single scenario. Broadening it to a matrix of injection types across all fault scenarios, and replacing the disjunctive keyword rule with a discriminating one, is required before the per-scenario scores can carry weight individually; the aggregate claim in this paper rests on the per-fault agreement pattern rather than on any single keyword match.

\textbf{Inference authorship and the blind re-run.} All diagnostic notes are authored by the reasoning agent from the statistical digests, with full evidence provenance. What is still missing is a comparison against \emph{human} experts on identical digests; an expert-vs-pipeline blind test is planned.

\textbf{Cost.} A scenario-level sweep spends minutes of reasoning per scenario---orders of magnitude more expensive than a fitted classifier at inference time. The economic comparison is only meaningful against the \emph{engineering cost of obtaining root causes} (expert hours), not against sample-level inference latency.

\textbf{Three-state honesty.} \textsc{competing\_set} and \textsc{needs\_data} conclusions (with capped confidence and named discriminating measurements) are a feature, not a failure. In this corpus all nine faults resolved to \textsc{determined} because the evidence differentiated cleanly; the discipline was exercised in a real deployment case (a biaxially-oriented polyester film line), where the pipeline returned \textsc{needs\_data} with an explicit discriminating-data list rather than fabricating a cause---episodic scratch defects whose driver lay outside the sensor set, a conclusion the statistic-heavy scans (180 parameter--defect associations, none surviving false-discovery-rate correction) supported quantitatively. Their systematic evaluation requires scenarios designed for indistinguishability, which we plan to add.

\subsection{Industrial deployment prospects}
Beyond the benchmark, the pipeline was engineered to fit the data and organisational realities of process plants, and its deployment surface is considerably wider than the evaluated scenarios.

\textbf{Integration with existing plant infrastructure.} IDD consumes what plants already produce: flat exports from historians (OPC-UA/PI), LIMS quality records, and batch logs. The same pipeline runs interactively through a web application and programmatically through a versioned REST API (\texttt{/api/diagnosis} with engine selection and typed error contracts), so a deployment is an export job plus one API call rather than an instrumentation project. Because the deterministic stage is byte-reproducible, results do not drift when the surrounding IT landscape changes.

\textbf{Data-sovereign deployment.} The fourteen-engine execution layer (Section~\ref{sec:arch}) accepts on-premise and private-model agents through uniform dialect adapters. For operators who cannot send process data to external LLM services---defence, pharmaceuticals, semiconductors, national infrastructure---the reasoning stage can be pointed at an internally hosted model without touching the audit, evidence, or gate contracts; the benchmark protocol itself executes identically against a scripted mock engine, which is how the full chain is exercised in CI.

\textbf{Conclusions that route work, not just verdicts.} The three-state protocol converts diagnosis into an engineering workflow: \textsc{determined} conclusions carry mechanism chains that point to corrective actions; \textsc{competing\_set} and \textsc{needs\_data} conclusions carry named discriminating measurements---in effect, work orders for targeted instrumentation or tests. The film-line deployment above illustrates the value: the \textsc{needs\_data} verdict redirected the investigation from blind parameter tuning to surface-inspection and equipment-event data, which is where the defect family's driver plausibly lies.

\textbf{Audit-ready documentation for regulated operations.} Because every conclusion is bound to graded evidence, physics checks, and a fingerprinted execution trace, the by-product of each run is an RCA report that satisfies the documentation expectations of regulated environments (e.g., GxP batch-deviation investigation and incident post-mortems), where ``who concluded what, from which data, with which confidence'' is the deliverable.

\textbf{Knowledge compounding across plants.} The ontology layer is an asset store, not a per-run artifact: variable semantics, physical principles, and confounder annotations published once for a plant or product family are reused by every subsequent diagnosis, and the evaluation contracts make each new labelled episode immediately reusable as a graded scenario. The marginal cost of a diagnosis therefore falls as the fleet grows---an economics that sample-level detectors, which must be retrained per asset, do not share.

\textbf{Scope of applicability.} The method requires multivariate time-ordered process data with engineering semantics for the channels (to build the ontology) and applies to continuous, batch, and testbed processes alike---the three benchmark families were chosen to span exactly that range. It does not apply where no time-aligned process data exists, and its root-cause reach is bounded by the sensor set, as the \textsc{needs\_data} case demonstrates honestly.

\section{Conclusion}
\label{sec:conclusion}

We presented IDD, an agentic diagnosis pipeline that turns LLM-based root-cause analysis into an auditable engineering procedure: competing hypotheses with mechanism classes, evidence levels bounding reported confidence, a four-condition anti-spurious filter, automatic physical verification, and quality gates with bounded repair. We further contributed a machine-checked reproducibility contract for diagnosis benchmarks---truth isolation enforced by an automated leakage sentinel, data fingerprinting, recorded inference, gate-graded scoring---executed it on a 12-scenario corpus across SKAB, TEP, and IndPenSim, and separated the reproducibility of the deterministic statistical layer from the inherent stochasticity of the reasoning layer. IDD achieved Top-1 root-cause accuracy of 100\% (CI $[70.1,100]$), CDR 1.00, perfect calibration, and zero false alarms on the reasoning-stage benchmark defined in Section~\ref{sec:scope}, with every result backed by an execution proof. On the per-fault TEP comparison it matches or exceeds both published single-LLM baselines without candidate lists---including two faults PCA cannot detect and one the GPT-4o baseline mis-diagnoses---while adding the evidence and calibration dimensions those baselines lack. The evaluation framework, per-scenario evidence chains, and reproducibility artifacts are released to support scrutiny and reuse. Three extensions are needed before the results can be read as definitive, and we state them as commitments rather than possibilities: end-to-end evaluation of the full multi-agent orchestration rather than its reasoning stage; an injection-based scoring-validity study alongside the novel-disturbance suite that closes the shortcut-learning question; and an independently computed quality-gate score to replace the self-reported one.

\section*{CRediT authorship contribution statement}
\textbf{Anonymous}: Conceptualisation, methodology, software, investigation, writing. (Full statements to be completed upon de-anonymisation.)

\section*{Declaration of competing interest}
The authors declare no competing interests.

\section*{Data availability}
The evaluation repository (scenario definitions with per-fault literature baselines, per-scenario inputs and fingerprints, blind-diagnosis briefs, inference notes, grading records, aggregation scripts, HTML/Markdown scoring reports, and the reproducibility report) is available at the project repository under \texttt{experience/}, \texttt{results/benchmark/} and \texttt{docs/benchmark/}. Third-party datasets are publicly available from their cited sources.

\bibliographystyle{elsarticle-num}
\bibliography{refs}

\end{document}
