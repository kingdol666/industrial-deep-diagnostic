# RAG Knowledge Builder — Python Dependencies
# Install: pip install -r requirements.txt

# Vector database (persistent, local, zero-config)
chromadb>=0.4.0

# Embedding model (384d, lightweight, local)
sentence-transformers>=2.2.0

# Optional but recommended
numpy>=1.24.0
