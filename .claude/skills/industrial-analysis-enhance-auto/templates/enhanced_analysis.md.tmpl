# 增强诊断分析报告

> 自动生成于 ${generated_at}  
> 运行标识: ${run_id}  
> 管线状态: ${enhancement_status_zh} (`${enhancement_status}`)

---

## 0. AI 可操作摘要（机器可读）

> **本节为下游 AI Agent 的主要消费接口。** 下方 JSON 块编码了分析结果的结构化摘要：
> 控制杠杆、因果路径、枢纽参数。Agent 可直接解析此 JSON 获取行动参考，
> 无需阅读全文。完整证据详见后续章节。

```json
${ai_actionable_summary}
```

---

## 1. 执行摘要

本报告由工业诊断增强管线自动生成，基于对原始诊断运行的深层数据分析、衍生特征构建、条件关系分析和物理机理桥接的综合结果。

### 数据源

| 属性 | 值 |
|------|----|
| 数据文件 | `${data_file}` |
| 行数 | ${data_rows} |
| 列数 | ${data_cols} |
| SHA256 | `${data_sha256}` |

### 增强管线状态

管线状态为 **${enhancement_status_zh}**。

${warnings_block}

### 分析概览

| 维度 | 数量 |
|------|------|
| 分析变量 | ${n_nodes} |
| 统计关系 | ${n_edges} |
| 多跳因果路径 | ${n_causal_pathways} |
| 控制杠杆 | ${n_control_levers} |
| 枢纽参数 | ${n_hubs} |
| 物理机理链 | ${n_mech_chains} |

### 上游产物

${source_artifacts_list}

---

## 2. 控制杠杆（行动参考）

> **此节是操作优化的核心。** 每个控制杠杆列出了该参数对下游质量目标的影响方向、
> 强度、置信度和物理验证状态。下游 Agent 应基于此节制定操作策略。

### 控制杠杆总览

${control_levers_table}

### 控制杠杆详细分析

每条杠杆附带机器可读 JSON 块，包含完整的下游效应、风险因素和操作域信息。

${control_levers_detail}

---

## 3. 参数影响矩阵

> 参数对质量目标的影响矩阵。颜色编码：🟢强正相关(≥0.5) 🟡中等正相关(0.3-0.5)
> ⚫弱相关(0.1-0.3) · 极弱/无关 | 🔴🟠 负相关同级别 | ✓ 物理验证通过。

${influence_matrix}

---

## 4. 多跳因果路径

> 从过程参数到质量目标的多跳因果传导路径。路径强度 = 各边 |r| 的乘积。
> 最小置信度 = 路径上所有边的最低置信度。共 ${n_causal_pathways} 条路径。

${causal_pathways_section}

---

## 5. 参数网络中心性

> 每个参数在网络中的影响力评估。出度 = 该参数影响多少其他参数；
> 影响力分 = 所有出边强度之和；枢纽参数(⭐) = 出度≥3或影响力≥1.0。

${parameter_centrality_table}

---

## 6. 物理上下文映射

> 每个参数在本体模型中的物理含义、单位、角色和控制方程。
> 此表建立了数据列名与物理量之间的映射。

${physical_context_table}

---

## 7. 管线溯源与覆盖范围

### 输入产物清单

本增强管线消费了以下基线诊断产物：

${source_artifacts_list}

### 变量覆盖

分析覆盖 ${n_nodes} 个变量，构建 ${n_edges} 条统计关系。

${nodes_table}

---

## 8. 关系图谱与统计证据

以下各节展示每条预测变量与目标变量之间的关系、统计证据和可操作性评估。每条核心结论附带机器可读的 JSON 证据块。

### 关联网络总览（数据驱动推理）

以下统计来自全变量两两扫描 + 推理引擎（时序领先、条件独立、变点同步、留一杠杆）：

${network_summary}

### 中介传导通道

${mediation_list}

### 本体矛盾警示

${contradiction_list}

### 可操作性分布

${operability_distribution_table}

---

${edges_sections}

---

## 9. 物理机理链

从诊断推理中提取并通过物理桥接验证的机理链，共 ${n_mech_chains} 条。

${mechanism_chain_sections}

---

## 10. 参数权衡矩阵

共 ${n_tradeoffs} 个参数在多目标间的权衡关系。

${tradeoff_sections}

---

## 11. 可操作性综合评估

${operability_summary_text}

### 详细分布

${operability_distribution_table}

---

## 12. 待解决问题

当前分析阶段未解决的关键问题，共 ${n_open_questions} 项。

${open_questions_list}

---

## 13. 证据缺口

当前证据体系中识别出的缺口，共 ${n_evidence_gaps} 项。

${evidence_gaps_list}

---

## 14. 附录：增强知识概要

以下为 `enhanced_knowledge.json` 的压缩视图，包含本文档中所有数据的机器可读表示。

```json
${appendix_json}
```
