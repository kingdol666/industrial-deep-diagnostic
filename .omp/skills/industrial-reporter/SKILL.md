---
name: industrial-reporter
description: "OMP registration stub for industrial-reporter. Canonical resources in .claude/skills/industrial-reporter. SKILL_PATH=../../../.claude/skills/industrial-reporter"
---

# industrial-reporter

OMP discovery stub. All resources (scripts, schemas, references, templates) live in:

- `.claude/skills/industrial-reporter/`

```bash
SKILL_PATH="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")/../../../.claude/skills/industrial-reporter" && pwd)"
SHARED_PATH="$(cd "$SKILL_PATH/../../shared" && pwd)"
```

Read `$SKILL_PATH/SKILL.md` for the complete skill protocol.
